# Склейка: очередь G

То, что осталось между «компоненты написаны» и «есть что сдавать». Наука закончена: датасет собран, модель обучена 18.08, метрики написаны. Здесь только интеграция.

Нумерация своя (G1, G2, …), как и у очереди F. Статус ведётся в таблице ниже.

| | Задача | Кто | Ждёт | Каталоги | Оценка | Статус |
|---|---|---|---|---|---|---|
| G1 | Паритет эталона на настоящей Model_Z | Савелий | — | `economics/` | 0.5 дня | готово 20.08 · c49173a |
| G2 | Один канон хеша и один порядок оси скважин | Савелий | — | `contracts/`, `schedule/` | 1 день | готово 20.08 · f4a3cf8 |
| G3 | Реальный `RunArtifact` вместо синтетики | Михаил | — | `ui/`, `frontend/public/data` | 0.5 дня | готово 20.08 · 24532fa |
| G4 | Гейт суррогата против CRM и слияние ветки | Андрей | — | `surrogate/` | 1 день | заблокировано 20.08 — CRM не кросс-сценарный, гейт технически не собрать без нового адаптера, см. разбор ниже |
| G5 | Первый собственный `Schedule*` | Андрей | G4 | `optimizer/`, `policy/` | 1–2 дня | в работе 20.08 — сквозной прогон θ → Schedule* собран и работает, validate_static зелёный, но ЧДД падает из-за нулевой λ-заглушки G3, сам запуск CMA-ES не выполнен |
| G6 | Тракт задачи 62, звено А | Андрей | G2 | `bridge/` | 2–3 дня | в работе 20.08 · 0a4e7ad — сервис и все шесть тождеств реализованы и протестированы, но приёмка буквально («тракт проходит целиком на базовом расписании») не выполняется: гейт `validate_dynamic` корректно блокирует базовое расписание, см. карточку |
| G7 | Финальный OPM-прогон и `FinalNpvArtifact` | Андрей | G5, G6 | `bridge/` | 0.5 дня | |

**G1, G2, G3, G4 запускаются одновременно.** Разные люди, разные каталоги, ноль пересечений по файлам. Это весь параллелизм, который здесь есть: G5 ждёт модель, G6 ждёт канон хеша, G7 ждёт обоих.

## Состояние машины, на котором построены оценки

Проверено 20.08 на рабочем ноутбуке: Docker 29.0.1 установлен и не запущен, 12 ядер, 24 ГБ памяти, 49 ГБ свободного диска. Датасет из 700 прогонов лежит локально в `aios-hack/dataset-700` (7.8 ГБ), базовый отклик OPM — в `dataset-700/base_run/runs/*/output/` (`MODEL_Z.SMSPEC`, `MODEL_Z.UNSMRY`).

**Сервер не нужен нигде, кроме G7 и опциональной массовой оптимизации.** G1–G6 считаются локально; G7 — один прогон OPM, на 12 ядрах ориентировочно 10–20 минут против 513 секунд на 32-ядерной машине.

**Внимание к диску.** Оборванный прогон Flow занимает около 2 ГБ до компактизации; при 49 ГБ свободных длительная серия прогонов требует присмотра.

---

## G1. Паритет эталона на настоящей Model_Z

**Цель.** Проверить схождение нашего `Economics` с эталонным расчётчиком организаторов на реальной выгрузке Model_Z, а не только на их учебном примере.

**Почему.** `economics/tests/test_reference_parity.py` доказывает совпадение до машинного нуля, но внешняя сверка идёт на `Пример_исходных_данных.xlsx` — **2 скважины и 24 интервала**. На 103 скважинах и 224 интервалах эталон не запускался ни разу. Между этими двумя случаями лежит всё, что может разойтись: расчёт `*_Diff` внутри скважины, правило исключения отрицательных строк, автомат ЭЦН с прокруткой от 1994, автомат состояний фонда, годовая база налога, масса нефти, собранная из `COPT`/`COPR` по двум `PVTNUM`. Бюджет расхождения — «пара процентов» на всё сразу, и ошибка в экономике съедает его целиком без права сослаться на разницу симуляторов.

**Дополнительный сигнал.** Наш базовый прогон даёт около 11 886 млн руб по замеру до этой задачи (точное значение, полученное здесь, — 11 873.68 млн). Просчитанный сценарий Аленькина `Модель2трек_Оптимальный_002_2_OPTIMIZED` из поставки эталона даёт порядка 10 588 млн на базе 2007. Ничегонеделание оказывается на 12% лучше сценария, названного оптимальным. Объяснений три: разные базы дисконтирования, разные сценарии, или дефект нашего расчёта. Задача обязана назвать, какое из них верно.

**Что сделать.**

1. Прочитать базовый отклик из `dataset-700/base_run/runs/*/output/` через `bridge/response_loader.py` — Docker для этого не нужен, файлы уже посчитаны.
2. Собрать входной файл в формате `REQUIRED_COLUMNS` расчётчика: `DATA, well, WLPT, WLPR, WOMT, WOMR, WWIR, WWIT, THP, BHP, WEFF` плюс `WLPT_Diff, WOMT_Diff, WWIT_Diff`, посчитанные нами по правилам §4.1.1.
3. Запустить эталон: `python3 РАСЧЕТ_ЧДД.py --start-year 2007` из `docs/models/CHDD_PYTHON/`.
4. Сравнить постатейно по годам и по месяцам, а не только итог. Совпадение итога при разошедшихся статьях — ложное схождение.
5. Записать в этот файл: величину расхождения, число разошедшихся статей и **объяснение дельты к сценарию Аленькина**.

**Приёмка.** Тест в `economics/tests/`, гоняющий сверку на базовом отклике Model_Z и падающий при расхождении выше машинной точности. Если расхождение обнаружено — тест фиксирует его величину и место, а задача переходит в разбор причины; закрывать её «зелёным» до объяснения нельзя.

**Сделано, 20.08.** Настоящий базовый прогон (`aios/data/base_run`, кеш) прочитан через `bridge.run_base_case` → `ResponseLoader`, артефакт сохранён в `aios/data/base_case/response.json` (103 скважины, 371 дата, 224 интервала). Тест `economics/tests/test_reference_parity_base_run.py`: **0 расхождений из статей года и месяца**, наш ЧДД `11 873 676 459.64` против эталонного `11 873 676 459.641228` — разница `1.9·10⁻⁶` руб, ULP double на сумме порядка `1.19·10¹⁰`, не расхождение методики. Схождение до машинного нуля подтверждено на полном масштабе, не только на учебном примере организаторов.

**Объяснение дельты к сценарию Аленькина.** Верны причины 1 и 2 из трёх, причина 3 (дефект нашего расчёта) исключена тестом выше.

1. **База дисконтирования.** Просчитанный сценарий Аленькина в `docs/models/CHDD_PYTHON/output/...AlenkinDA.json` посчитан с `startDate=1991-01-01` (`totalChddM=4418.13` млн) — расчётчик взял год первой строки исходных данных, `--start-year` не передавался. Пересчёт той же годовой таблицы `annual[].fcfM` на базу 2007 (`Σ fcfM_year / (1+0.10)^(year−2007)` для `year ≥ 2007`, `waccRate=10%` из `assumptions` того же JSON) даёт **10 588.42 млн руб** — воспроизводит и уточняет прежнюю оценку «≈10 588 млн» из `04_models.md` §4.1 (там она получена грубее, суммой недисконтированных `FCF`). Исходного CSV сценария Аленькина в репозитории нет — пересчёт средствами `chdd_model.compute_calculation` с `start_date=2007-01-01` невозможен, годовая таблица из `output/*.json` — единственное, что доступно.
2. **Разные сценарии, не одно и то же расписание.** Даже на общей базе 2007 остаётся разрыв: наш базовый прогон (нулевая перекладка) даёт `11 873.68` млн, сценарий Аленькина на той же базе — `10 588.42` млн, разница `1 285.26` млн (**12.1%**) — величина и знак совпадают с «на 12% лучше», отмеченным выше. Сравниваются два разных объекта: наш прогон — Model_Z с нулевой перекладкой (103 скважины, горизонт 2007–2025 по деку организаторов), сценарий Аленькина — просчитанный вход `Модель2трек_Оптимальный_002_2_OPTIMIZED` с историей 1991–2025 и объёмами (`totalOilKt=4771`, `totalRevenueM=133 595` за все 33 года), которые не сопоставлены напрямую с масштабом Model_Z ни в одном документе базы знаний. Статус референса организаторы этому сценарию не давали (`06_open_questions.md` п.7), и без исходного CSV нельзя подтвердить, что это то же месторождение/тот же горизонт скважин, а не другой промысловый объект с тем же названием методики расчёта. «Ничегонеделание лучше оптимального» — не аномалия расчёта, а следствие сравнения разных объектов на приблизительно уравненной базе дисконтирования.

---

## G2. Один канон хеша и один порядок оси скважин

**Цель.** Устранить два расхождения, из-за которых `canonical_schedule_hash` не является единственным.

**Почему.** Обе проблемы записаны в `../../context/06_open_questions.md` §3 п.16 и п.18 и обе блокируют доказуемый provenance финальной сдачи.

1. `contracts/hashing.py` называет `canonical_bytes` «практическим приближением» RFC 8785: числа сериализует `json.dumps`, поэтому `45.0` печатается как `45.0`, а JCS требует `45`. Строгая реализация в `schedule/canonical.py` даёт **другой хеш на одном и том же расписании**.
2. `contracts/schedule.py` объявляет `meta.wells` в лексикографическом порядке, `schedule/build.py` и `schedule/canonical.py` строят ось числовой сортировкой. На Model_Z это **102 переставленные позиции из 103**. Порядок входит в хеш.

Плюс второй дефект той же сериализации, найденный задачей 53: `_to_jsonable` не обрабатывает enum в позиции ключа словаря, из-за чего `canonical_bytes(config)` падает на `Config.rules`.

**Что сделать.** Выбрать один канон по каждому пункту и применить во всех пакетах. Рекомендация: строгий JCS из `schedule/canonical.py` как канон проекта, порядок оси — тот, который уже требует `OpmDeckEmitter`, чтобы не переписывать мост. Решение записать в журнал `../../context/07_concept.md` §9 строкой с причиной.

**Кто.** Только интегратор — задача правит `contracts/`.

**Приёмка.** Тест: `contracts.hash_schedule()` и строгая реализация дают одинаковое значение на базовом расписании; `canonical_bytes(config)` не падает на enum-ключах; ось скважин одинакова в `contracts`, `schedule` и `bridge`; кеш прогонов на 700 существующих записях не инвалидирован — либо инвалидирован осознанно, и это записано здесь.

**Сделано, 20.08.** Строгий JCS перенесён в `contracts/hashing.py`, `schedule/canonical.py` переиспользует его. Ось скважин — лексикографическая везде, канон `bridge.OpmDeckEmitter.source_wells`; правки в `schedule/build.py`, `schedule/canonical.py`. Полный прогон тестов вскрыл третий, независимый numeric-сортировщик в `bridge/dataset_plan.py` (использовался при материализации сценариев датасета для перебора событий по скважинам) — без синхронизации с ним `materialize()` базового сценария давал другой хеш, чем сам базовый `Schedule`; приведён к тому же канону. `canonical_bytes(config)` больше не падает на enum-ключах (задача 53). Новый тест — `contracts/tests/test_hash_canon.py`.

**Инвалидация кеша — осознанная, без миграции.** `RunCache` (`bridge/cache.py`) ключуется тройкой `deck_hash + canonical_schedule_hash + summary_hash`. Смена канона (число + порядок оси) меняет `canonical_schedule_hash` для любого расписания, построенного из числового представления — то есть существующие записи кеша под этим ключом больше не находятся. Проверено: `dataset-700/base_run/cache/` пуст (0 записей — инвалидировать нечего), `aios/data/base_run/cache/` содержит одну запись базового прогона. Дорогая часть (сами `MODEL_Z.SMSPEC`/`.UNSMRY` на диске) не тронута — следующий вызов `run_base_case` просто промахнётся по кешу один раз и пересчитает ключ, без повторного прогона Flow. Миграция существующих записей на новый ключ не делалась: цена написания мигратора для одной записи выше цены её пересчёта.

---

## G3. Реальный `RunArtifact` вместо синтетики

**Цель.** Убрать `"provenance": "synthetic-demo"` из интерфейса.

**Почему.** Находка D2 аудита: во фронте лежит демонстрационный бандл с ЧДД 141 177 руб при реальном базовом прогоне 11 873.68 млн. Пометка честная, но на вопрос «это настоящий расчёт?» ответ отрицательный. Это же предусловие для всей очереди F11–F16.

**Что сделать.** Собрать `RunArtifact` из локального базового прогона (`dataset-700/base_run`) средствами `ui/` и положить в `frontend/public/data`. Оси берутся из артефакта, не из литералов. Демонстрационный бандл сохраняется отдельным сценарием библиотеки и остаётся помеченным.

**Приёмка.** Тест: в собранном бандле `synthetic` равно `false`, `npv_methodology` совпадает со значением из расчёта на базовом отклике, число скважин и шагов взято из данных. Фронт открывается без `SyntheticBanner` на основном сценарии.

**Сделано, 20.08.** Новый `ui/base_artifact.py::build_base_artifact` собирает `RunArtifact` сценария `base` из `aios/data/base_case/response.json` (задача G1) через `economics.analyze_base_case` — `schedule`, `npv_table`, `state_at_date`/`interval_response` настоящие, `provenance="model-z-base-run"`, `synthetic=false` вместо `"synthetic-demo"` с ЧДД 141 177 руб. `whatif-injection-cut` остаётся демо-библиотекой, честно помеченной. `SyntheticBanner`/`ProvenanceContext` во фронте правки не потребовали — уже читают `meta.synthetic`/`meta.provenance` из данных. `frontend/public/data` не в git (`frontend/.gitignore`), коммитится только код-генератор.

**Отступление от карточки — groups/λ.** Настоящую связность (`groups`/`lambda_`) из одного базового прогона честно не посчитать: нужна серия экспериментов с отклонениями закачки (`connectivity`, ортогональный план), которой нет. Заполнено тривиальной, но не фиктивной заглушкой — одна группа на весь фонд, нулевая матрица влияния правильной формы, хеши настоящие (`connectivity.groups.lambda_hash`/`group_hash`), помечено в докстринге `ui/base_artifact.py`. Не выдаётся за измерение.

**Отступление от карточки — `final_npv` не заполнен.** `FinalNpvArtifact` — «единственный разрешённый источник заявленного числа» (README §6a), подразумевает прохождение полного тракта сдачи (задача 62/G6: `validate_static → OpmDeckEmitter → Runner → status==OK → ResponseLoader → validate_dynamic → Economics → FinalNpvArtifact`, шесть тождеств хешей §10.5). Тракта ещё нет, поэтому `scenarios.json` показывает `submitted: null` для обоих сценариев — заполнять `final_npv` сейчас значило бы заявлять то, что не проверено тем же классом ошибки, что и `synthetic-demo`.

**Что осталось после G6.** Когда тракт задачи 62 появится (G6/G7), `build_base_artifact` — кандидат на замену источником `FinalNpvArtifact` из настоящего прогона, и `groups`/`lambda_` — на замену реальной оценкой связности, когда появится серия экспериментов.

Новые тесты: `ui/tests/test_base_artifact.py` (приёмка дословно), `ui/tests/test_demo.py` — раздельная разметка `base`(реальный)/`whatif`(демо).

---

## G4. Гейт суррогата против CRM и слияние ветки

**Цель.** Формально закрыть задачу 36 и влить модель в `main`.

**Почему.** Модель обучена 18.08 и лежит вне `main`. Гейт «суррогат обязан бить CRM по ранговой корреляции» не запускался: `compare_to_baseline` написан и не вызван. Пока этого нет, суррогат нельзя ни защищать, ни использовать в G5.

**Что сделать.**

1. `git checkout feat/surrogate/34-700`. **Extras `ml` объявлен только в этой ветке**, в `main` его нет — `pip install -e ".[dev,ml]"` из `main` упадёт.
2. Перенести датасет: `cp -R ../dataset-700/dataset-main ../dataset-700/dataset-extra-500 data/`.
3. Прогнать `compare_to_baseline` на **тех же 105 отложенных сценариях**, на которых считался `training_report.json`. Сравнение на других сценариях недействительно.
4. Записать результат сюда: бьёт или не бьёт, с числами.
5. Влить ветку в `main` через PR — вместе с extras `ml` и описанием модели.

**Честная развилка.** Если суррогат CRM не бьёт — это результат, а не провал задачи. Тогда он сдаётся как измеренная неудача с объяснением, CRM остаётся базовой линией, а G5 переключается на оптимизацию поверх CRM либо прямо на OPM. Переобучение с ranking loss — работа третьего приоритета, не в этой очереди.

**Приёмка.** Отчёт сравнения на 105 сценариях с ранговой корреляцией обеих моделей; ветка в `main`; тесты `surrogate/` зелёные.

**Заблокировано, 20.08.** Шаг 3 невыполним в том виде, как описан карточкой. `surrogate/crm.py` — не кросс-сценарный предиктор ЧДД: `CrmModel` подгоняет коэффициенты аллокации на **префиксе интервалов управления одного расписания** и меряет качество на **суффиксе того же расписания** (`CrmSplit.train_steps`/`holdout_steps`). Между 105 разными тестовыми сценариями CRM ничего не переносит — это прямо зафиксировано в `SURROGATE_HANDOFF.md` §3: «ранговой корреляции по ЧДД здесь нет и быть не может: сценарий один, ранжировать нечего».

`accept_against_baseline`/`compare_to_baseline` (`surrogate/metrics.py`) требуют на входе `NpvRankingMetrics` для CRM — то есть предсказанный CRM ЧДД по каждому из 105 разных расписаний. Функции, которая превращает CRM в такой предиктор, в репозитории не существует: ни кода, ни теста, ни наброска. Правдоподобный путь (подгонять CRM отдельно на раннем префиксе каждого из 105 сценариев, экстраполировать поздний суффикс, из экстраполяции жидкости достроить `StateAtDate`/`IntervalResponse` — CRM отдаёт только `liquid_rate`, не обводнённость и не состояние фонда — и только потом считать ЧДД через `Economics`) — это отдельная задача по объёму, не подзадача внутри G4, и не описана нигде как принятое решение.

**Решение — гейт не запускать, дыру не закрывать угадыванием.** Модель 18.08 (`model_version` `0ba51419…`) остаётся невлитой в `main`, вердикт `accepted`/`rejected` по §5.5 не выносится ни в какую сторону — не потому что модель проиграла CRM, а потому что сравнение технически не собрано. Реконструкция тестовой выборки проверена на уровне кода (не выполнена): `surrogate/train.py:split_samples` детерминирован по `seed=20260817` над `pilot.samples(seed=20260816, PILOT_CONFIG) + extra.samples(seed=20260817, EXTRA_CONFIG)` (`surrogate/cycle.py`) — сами 105 сценариев воспроизводимы, когда адаптер появится.

Что осталось перед повторной попыткой: либо завести отдельную задачу на кросс-сценарный CRM-адаптер (с явным описанием, как из голого `liquid_rate` получать деньги), либо пересмотреть §5.5 на «гейт против наивной базовой линии другого вида» и записать это решением в `../../context/07_concept.md` §9 — обе развилки требует интегратора, самовольно не решается.

---

## G5. Первый собственный `Schedule*`

**Цель.** Получить расписание, которое построила наша система, а не организаторы.

**Почему.** Это главная дыра проекта: 61 задача закрыта, а система ни разу не приняла ни одного собственного решения. Сегодня лучшее, что можно сдать, — расписание организаторов, пересчитанное нами. Критерий «эффективность подхода: NPV против референсного сценария» при этом получает ноль.

**Что сделать.** Запустить CMA-ES (`optimizer/search.py`) поверх суррогата через `optimizer/interface.py`, с разрешением состояния неподвижной точкой (`policy/fixed_point.py`). Прогон суррогата — доли секунды, **симулятор не нужен**. На выходе: `θ*`, `Schedule*`, `Trace`, флаги `converged` и `self_consistent`.

**Чего не ждать.** `precision@1` суррогата равен `0.0` при `regret@1` в 201 млн руб. Предсказанному ЧДД верить нельзя — честное число даст только G7. Задача даёт **план**, а не число.

**Приёмка.** `Schedule*` проходит `validate_static` без нарушений, отличается от базового расписания, канонический хеш зафиксирован, `Trace` заполнен. Записать сюда: сколько итераций, сошлась ли неподвижная точка, предсказанный ЧДД и его дельту к базовому.

**В работе, 20.08.** Написана связка `optimizer/schedule_search.py` — раньше нигде не существовавший сквозной прогон θ → `Schedule*` (суррогат + `policy/hierarchy.py` R0–R7 + `policy/fixed_point.py`). Готовые куски (`resolve`, `optimize`, `run_step`, `apply_all`) не были собраны воедино ни разу; по пути закрыто две дыры, которых не было в карточке:

1. `RuleContext.group_injection_m3_per_day`/`group_offtake_m3_per_day` (нужны R5) ни один существующий вызывающий код не заполнял — добавлена агрегация по группе (`_group_injection_offtake`) тем же способом, что R1 агрегирует спрос.
2. Несколько правил в одном шаге могут предложить `SET_LRAT`/`SET_RATE` для одной скважины с разными значениями (R2 задаёт уровень, R4 его же ограничивает) — `canonicalize_control_events` видит это как конфликт и падает. Решение: последнее правило по порядку `IMPLEMENTED_RULES` выигрывает (`R4`/`R6` по смыслу ограничивают то, что предложили `R1`/`R2`), реализовано словарём `pending` на `(control_step, well, kind)`.

Проверено вживую на базовом расписании и настоящем чекпойнте (`model_version` `0ba51419…`): `Schedule*` на `θ=default_theta()` строится на всех 224 шагах, 18279 управляющих событий, отличается от базового, `validate_static` — 0 нарушений. Неподвижная точка (4 итерации, ~2.9 с/итерация) **не сходится** и ЧДД падает: 4.26 → 3.70 → 3.40 → 3.27 млрд руб против настоящих 11.89 млрд на базовом.

**Падение ЧДД — не дефект этого файла, а честное следствие заглушки G3.** `Groups`/`Lambda` здесь та же тривиальная заглушка, что в G3: одна группа на весь фонд, нулевая матрица влияния. При λ=0 правило R1 «вода течёт туда, где превращается в нефть» получает нулевую предельную ценность закачки везде и душит её по всему фонду — без настоящей λ система физически не может распределять воду осмысленно. Это ограничение унаследовано от заглушки, а не изобретено здесь.

**Что осталось до зелёной приёмки.** Сам запуск CMA-ES (`optimizer.search.optimize` поверх `Objective`) — код к нему готов (`nominal`/`provenance` замыкания поверх `resolve`), но не выполнен: неясно, есть ли смысл гонять поиск поверх заведомо сломанной (нулевой) λ, или это даст мусорный результат, который придётся переделывать после того, как λ станет настоящей. Решение — за тем, кто продолжит.

---

## G6. Тракт задачи 62, звено А

**Цель.** Один сервис, превращающий `Schedule*` в `FinalNpvArtifact` с доказуемым происхождением.

**Почему.** Задача 62 — единственная незакрытая позиция блока сдачи. Части существуют, сервиса нет, поэтому реальный `final_npv` не производится ничем.

**Что сделать.** Собрать цепочку по `../../context/08_contracts.md` §10.5: `validate_static` → `OpmDeckEmitter` → `Runner` → проверка `status == OK` → `ResponseLoader` → `validate_dynamic` → `Economics` → `FinalNpvArtifact`. Реализовать все шесть обязательных тождеств хешей из таблицы §10.5.

**Что можно сделать без Docker.** Всё, кроме самого вызова Flow: тракт пишется и тестируется на кешированном базовом прогоне из `dataset-700/base_run`, кеш ключуется тройкой хешей и настоящий запуск не потребуется.

**Приёмка.** Тест на базовом расписании: тракт проходит целиком, все шесть тождеств выполняются, подмена расписания после прогона ловится, `status != OK` останавливает выдачу числа.

**Реализовано, 20.08 — приёмка не пройдена буквально, см. находку ниже.** `bridge/submission.py::submit_schedule` — `validate_static` → `OpmDeckEmitter` → `CachingOpmRunner` → `status == OK` → `ResponseLoader` → `validate_dynamic` → `Economics` → `FinalNpvArtifact`, все шесть тождеств §10.5 проверяются явно (`SubmissionTractError` с указанием, какое именно не выполнено). `methodology_version_hash` — новый `economics/methodology_hash.py`: sha256 по байтам файлов с формулами (`npv.py`, `ledger.py`, `decomposition.py`, `fund.py`, `esp.py`), не строка-версия — правку формулы не нужно вручную помечать, хеш меняется сам. `economics_config_hash` переиспользует уже существовавший `config.economics_config_hash`, не переизобретён. Тест `bridge/tests/test_submission.py`, без Docker: «Schedule*» — расписание с настоящим воспроизведённым `initial_state` поверх тех же control/fixed-событий, что уже прогнаны в кеше `aios/data/base_run` (байт-в-байт совпадение дека проверено, не предположено).

**Важная находка — приёмка карточки была неверна в одном пункте.** «Тракт проходит целиком... на базовом расписании» не выполняется сегодня: гейт `validate_dynamic` корректно останавливает базовое расписание организаторов на 71 нарушении. 60 из них — `OPEN_WITHOUT_FLOW` по скважине `71` (открыта с уставкой на шагах 0–59 до своей перфорации на шаге 60) — это ровно открытый вопрос №17 (`../../context/06_open_questions.md`), уже закреплённый отдельным тестом `schedule/tests/test_validate_dynamic.py::test_real_response_open_without_flow_stops_at_first_perforation`, который ожидает эти нарушения, а не считает их браком. Оставшиеся 11 — `BHP_LIMITED_WITHOUT_UNDERSHOOT` (10) и `BHP_BELOW_PRODUCER_LIMIT` (1) на 10 разных скважинах — новая находка, число раньше никто не считал по полному тракту. **Значит: ни одно `Schedule*`, унаследовавшее уставку скважины 71 как есть, сегодня не пройдёт звено А до конца**, пока трекер не ответит на вопрос №17 или пока `Schedule*` не поправит скважину 71 явно (гибридная поправка). G5 и G7 должны знать об этом заранее, а не наткнуться на защите: G5's `Schedule*`, если он не трогает уставку скважины 71, тоже упрётся в этот гейт.

`report_undershoot=False` — осознанный выбор, не ослабление гейта: `BHP_LIMITED` — легитимный режим контроля («скважина упёрлась в предел 50/300 бар, цель недостигнута», `08_contracts.md` §8.1), недостижение цели из-за настоящего физического предела само по себе не нарушение; `BHP_LIMITED_WITHOUT_UNDERSHOOT` (нестыковка режима и факта) и прямые пределы BHP остаются жёсткими проверками.

**Дополнено 20.08 — отчёт вместо обрыва на первом сбое (`40658bf`).** Из закрытой ветки `feat/andrey/62` перенесён её единственный содержательный вклад: звено А считает **все шесть тождеств §10.5 целиком**, а не падает на первом расхождении. `IdentityCheck{name, holds, detail}` на каждое, `SubmissionResult` несёт `identities`, `failed_identities`, `sound` и свойство `npv_methodology`, недоступное пока цепочка не прошла. Несошедшийся прогон больше не обрывает тракт исключением — отклик, динамика и ЧДД становятся `None`, и это видно в отчёте; ЧДД считается и при грязной динамике (заявлять нельзя, но иначе нарушение динамики неотличимо от ошибки в деньгах). Единственный досрочный обрыв — `validate_static`, за ним прогон OPM ценой 10–20 минут для уже отклонённого расписания. `strict=False` возвращает отчёт без исключения — для разбора, не для сдачи. Причина переноса: попытка сдачи одна, и различать «сломано одно» и «цепочка разошлась целиком» надо до неё, а не после. Ветка `feat/andrey/62` после переноса удалена.

`ArtifactHashes.groups_hash/dataset_version_hash/surrogate_checkpoint_hash` в тесте — заглушки (`"0"*64`), G4/G5 ещё не дали настоящих значений; `deck_hash`/`history_prefix_hash`/`summary_spec_hash` — настоящие.

---

## G7. Финальный OPM-прогон и `FinalNpvArtifact`

**Цель.** Получить настоящее число для `Schedule*`.

**Что сделать.** Запустить Docker, прогнать `Schedule*` из G5 через тракт G6, получить `FinalNpvArtifact`. Сравнить с базовыми 11 873.68 млн (замер G1, тест `economics/tests/test_reference_parity_base_run.py`).

**Развилка после результата.** Если собственный план хуже базового — сдаём базовое расписание и честно говорим, что оптимизация улучшения не дала; это лучше, чем сдать худшее число. Если лучше — запускать серию прогонов CMA-ES прямо на OPM: 6 воркеров (Flow сам многопоточный, воркеров ставить около половины ядер), ориентировочно 10–15 прогонов в час, за трое суток порядка 1000 — этого достаточно для поиска по 10 параметрам без суррогата вообще.

**Приёмка.** `FinalNpvArtifact` существует, тождества зелёные, число записано сюда вместе с `run_id`.

**Результат 20.08.** Прогон выполнен: Flow `OK`, звено А целиком за 8.3 минуты, все шесть тождеств §10.5 сошлись, `run_id` `20260820T152808-94f0303bb89e` и следующий за ним. ЧДД по настоящему прогону **10.805 млрд** против 11.874 базовых, то есть минус 9.0 процента. `FinalNpvArtifact` не выдан: `validate_dynamic` даёт 77 нарушений, `sound=False`. Развилка карточки сработала в сторону «план хуже базового» — сдавать пока нечего, кроме базового расписания.

---

## G9. Куда делся миллиард

**Цель.** Назвать статьи, на которых наш план проигрывает базовому, числами.

**Почему.** Разрыв 1.07 млрд руб (10.805 против 11.874) — единственное, что отделяет систему от результата. Причина неизвестна: постатейного сравнения никто не делал, и любое улучшение правил сейчас делается вслепую. Всё остальное в очереди — техника; это продукт.

**Что сделать.** Взять отклик нашего прогона (`aios/data/g7-submission`, кеш) и отклик базового (`aios/data/base_case/response.json`), прогнать оба через `economics.analyze_base_case` и сравнить разложения: по годам, по скважинам, по статьям затрат. Отдельно посмотреть натуру, а не только деньги: накопленная добыча нефти и жидкости, накопленная закачка, число событий остановки и запуска, число замен ЭЦН, число скважин в работе по шагам.

Гипотезы, которые надо подтвердить или отвергнуть числами, а не рассуждением:
- правило R3 глушит убыточные скважины раньше, чем следует, и теряет позднюю нефть;
- закачка распределяется не туда: суммарно её меньше базовой (1196 против ~2000 м³/сут на шаг у базового плана), значит давление падает;
- событийные затраты съедают выигрыш: наш план даёт 43 190 управляющих событий против 18 279 у базового, а каждая смена состояния действующего фонда стоит 1.0 млн руб.

**Приёмка.** Таблица «статья — базовый — наш — разница» с суммой, сходящейся к 1.07 млрд. Названа доминирующая статья. Вывод записан сюда одним абзацем.

---

### Результат 21.08

Разрыв разложен и сводится точно. Отклик нашего плана взят из кеша
`aios/data/g7-submission`, базовый — `aios/data/base_case/response.json`, оба
прогнаны через `economics.analyze_base_case`. Числа недисконтированные, кроме
последней строки; сумма `discounted_fcf` даёт −1.068 млрд, то есть ровно
заявленный разрыв.

| статья | базовый | наш | разница |
|---|---|---|---|
| revenue | 102.491 | 92.489 | **−10.003** |
| deductions | 71.744 | 64.742 | −7.002 |
| income_tax | 6.854 | 6.196 | −0.657 |
| opex_liquid | 0.904 | 0.751 | −0.153 |
| opex_injection | 0.350 | 0.225 | −0.125 |
| event_costs | 0.138 | 0.061 | −0.077 |
| capex_esp | 0.027 | 0.012 | −0.015 |
| opex_oil | 0.146 | 0.132 | −0.014 |
| opex_wellstock | 1.794 | 1.792 | −0.002 |
| ebitda | 27.415 | 24.785 | −2.630 |
| fcf | 20.534 | 18.577 | −1.957 |
| **discounted_fcf** | **11.874** | **10.805** | **−1.068** |

**Доминирующая статья — выручка, и она единственная.** Все затраты у нашего
плана ниже базовых: opex суммарно −0.294, событийные −0.077, ЭЦН −0.053.

Натура объясняет, откуда выручка: нефть 3660.4 → 3303.2 тыс. т (−357.2),
жидкость 9036.1 → 7506.8 тыс. м³ (−1529.3), закачка 11670.0 → 7513.4 тыс. м³
(**−4156.6, то есть −35.6%**), действующий фонд 21534 → 21505 скв·мес (−29).
События: переводов 9 → 1, замен ЭЦН 39 → 18, остановок и запусков 23 → 24,
вводов 23 → 23. Строк, выброшенных правилом отрицательного прироста: 0 → 9.

По скважинам: 36 в плюсе на +1.028 млрд, 67 в минусе на −2.096 млрд. Худшие —
97 (−141.7 млн), 55 (−115.1), 23 (−98.0); лучшие — 10 (+278.7), 2 (+144.9),
51 (+92.0).

**Вывод.** Из трёх гипотез карточки подтвердилась ровно одна — вторая. R3
скважины раньше времени не глушит: действующий фонд отличается на 29 скв·мес
из 21534, то есть на одну десятую процента. Событийные затраты выигрыш не
съедают: их у нас вдвое меньше базовых, потому что 43 190 управляющих событий
— это записи уставок, а платится только за смену состояния действующего фонда,
и таких смен у нас меньше. Проигрывает план на закачке: он подаёт на треть
меньше воды, чем базовый, теряет на этом 9.8% нефти, а сэкономленный opex
покрывает от силы четверть потерянной маржи. Правило R1 на измеренной λ
недокачивает, и чинить надо его, а не затратную часть.

---

## G10. Внешний цикл верификации вместо доверия суррогату

**Цель.** Отбирать кандидатов настоящим прогоном, а не первым местом суррогата.

**Почему.** Замерено 20.08: CMA-ES поднял предсказание с 10.604 до 11.262 млрд, то есть выиграл 658 млн; настоящий прогон на том же плане дал 19 млн. Разница в тридцать четыре раза, ошибка предсказания перевернула знак с −1.7 на +4.2 процента. Поиск улучшает согласие с моделью, а не план. `precision@1` модели равен нулю — это ровно тот случай, ради которого §10.1 требует верификации.

**Что сделать.** Взять `bridge/verification.py` (написан, ни разу не калиброван) и собрать цикл: суррогат отбирает top-k по предсказанному ЧДД, OPM считает эти k, лучший по факту становится точкой следующего шага поиска. Начать с малого k (3–5) и бюджета в 20–30 прогонов, чтобы измерить, сколько настоящих прогонов стоит один шаг улучшения.

**Что записать.** Таблицу «предсказано — фактически» по всем проверенным кандидатам: она же требуемые к сдаче метрики качества суррогата (§10.3), отдельно считать не нужно.

**Приёмка.** Цикл отработал, в отчёте есть пары предсказание/факт, и по ним видно, растёт ли фактический ЧДД от шага к шагу.

---

### Результат 21.08: замер сделан, цикл не собран

Собран пул CMA-ES тем же seed 20260816 и тем же бюджетом 120, что дал θ\* —
первое место воспроизвелось, `canonical_schedule_hash` `c16b10564df4…` сошёлся
с `cmaes.json`. Из истории поиска взяты top-40 по предсказанию, каждый
восстановлен полным потолком неподвижной точки и **прогнан настоящим OPM**:
40 прогонов, все `RunStatus.OK`. Таблица целиком — `aios/data/g10-verification/table.json`.

| величина | значение |
|---|---|
| Spearman предсказания против факта | **0.0036** |
| Pearson | 0.157 |
| средняя абсолютная ошибка | 0.444 млрд |
| смещение | **+0.444 млрд** |
| место истинного чемпиона | **39 из 40** |
| regret@1 | 32.5 млн руб |
| regret обнуляется на | K = 39 |
| разброс предсказаний по top-40 | 32 млн руб |
| разброс фактических ЧДД | 118 млн руб (10.720…10.838) |

**Тезис «суррогату шортлист, выбор за Flow» не подтверждается.** Спирмен на
нуле — это не слабая связь, а её отсутствие: порядок внутри шортлиста модель
назначает случайно. Чемпион по физике оказался на 39 месте из 40, то есть
поймать его через top-K можно только прогнав почти весь пул, и шортлист
перестаёт быть шортлистом. Плато regret между K=5 и K=15, замеренное на
тестовом наборе чекпоинта, здесь выродилось в плоскую линию по всей длине.

Ошибка при этом почти целиком систематическая: MAE равна смещению, то есть
модель завышает **каждого** кандидата примерно на 444 млн, а не путается в них
по отдельности. Разброс предсказаний по всему top-40 составил 32 млн при
собственном шуме модели 210 млн — весь шортлист умещается в седьмую часть шума.

Лучший кандидат по факту — 10.838 млрд, всё ещё на 1.036 млрд ниже базового.

**Приёмка карточки не выполнена.** Сделан один раунд, а не цикл: `retrain`
и критерий сходимости §10.4 не подключены, роста фактического ЧДД от шага к
шагу измерить не на чем. Строить цикл поверх модели с нулевым Spearman по
шортлисту, впрочем, бессмысленно — сначала нужна другая модель, а она упирается
в невоспроизводимость датасета (см. «Вне очередей»).

### Побочно: блокер скважины 71 зависит от θ, а не от дека

Детализация нарушений по кандидатам (`aios/data/g10-verification/`, скрипт
`tools_g10_violations.py`):

| кандидат | `OPEN_WITHOUT_FLOW` на скв 71 | всего нарушений | ЧДД по OPM |
|---|---|---|---|
| 0 (θ\*) | 60 | 77 | 10.805 |
| 9 | **2** | 16 | 10.720 |

Тот же дек, та же скважина — 58 из 60 нарушений снимаются другой θ ценой
85 млн ЧДД. Значит G12 не обязана ждать ответа трекера: поправка достижима
внутри поиска. Ещё две устойчивые находки: `BHP_ABOVE_INJECTOR_LIMIT` — это
**скважина 12, ровно одно нарушение, во всех сорока кандидатах**, а
`NEGATIVE_INTERVAL_DELTA` держится за один и тот же набор скважин 31, 4, 35,
19, 21. Обе — материал для G11.

---

## G11. Семнадцать своих нарушений динамики

**Цель.** Довести собственные нарушения `validate_dynamic` до нуля.

**Почему.** Из 77 нарушений прогона G7 шестьдесят приходятся на скважину 71 (вопрос по деку, G12), остальные семнадцать — наши: 9 `NEGATIVE_INTERVAL_DELTA`, 7 `BHP_LIMITED_WITHOUT_UNDERSHOOT`, 1 `BHP_ABOVE_INJECTOR_LIMIT`. Пока они есть, `sound=False` и число не заявляется, даже если по скважине 71 придёт ответ.

**Что сделать.** Разобрать каждый вид по деталям нарушения: скважина, шаг, величина. `NEGATIVE_INTERVAL_DELTA` — вероятно тот же переток нефти обратно в пласт у почти остановленной скважины, что описан в `aios/SURROGATE_HANDOFF.md` §6; если так, решение уже принято там и его надо распространить. `BHP_ABOVE_INJECTOR_LIMIT` в единственном экземпляре — скорее всего одна скважина, которой план просит больше, чем она берёт при 300 барах.

**Приёмка.** Повторный прогон тракта даёт ноль нарушений, кроме тех, что принадлежат скважине 71.

---

## G12. Скважина 71: решение вместо ожидания

**Цель.** Снять блокер, который держит заявляемое число, не дожидаясь трекера.

**Почему.** 60 нарушений из 77 — это скважина `71`, открытая с уставкой на шагах 0–59 при первой перфорации на шаге 60. Вопрос организаторам не отправлен и может остаться без ответа. Ждать до 11.09 нельзя: без нуля нарушений `FinalNpvArtifact` не выдаётся никогда.

**Что сделать.** Отправить вопрос (это не агентская работа, `Вне очереди G`), и параллельно подготовить явную поправку в `Schedule*`: держать скважину `71` закрытой до шага 60, то есть до её первой перфорации. Поправка — наше решение о собственном плане, а не правка дека организаторов, и запрет подмены данных она не нарушает. Записать её отдельным решением в `../../context/07_concept.md` §9.

**Приёмка.** Прогон тракта на поправленном `Schedule*` даёт ноль нарушений `OPEN_WITHOUT_FLOW`, `sound=True`, `FinalNpvArtifact` существует, число записано сюда вместе с `run_id`.

---

---

## Вне очереди G, но параллельно и не агентом

- **Три вопроса трекеру одним сообщением:** точный синтаксис `wells_schedule.inc` (блокирует 62B), скважина 71 (открыта с уставкой 60 шагов до своей перфорации), 79 исчезнувших записей `SHUT` в заканчивании. Первый ответ разблокирует единственный обязательный сдаваемый артефакт.
- **Задача 65:** регистрация в GitLab организаторов и зеркалирование репозитория.

---

## Промт для запуска сессии

Подставить номер задачи и имя, остальное не трогать:

```
Ты работаешь в репозитории aios, сессия <ИМЯ>.

Прочитай CLAUDE.md этого репозитория и следуй ему.
Прочитай ../docs/v2/AUDIT-20.08.md целиком — это состояние на вечер 20.08
с числами: что уже доказано прогонами, а что нет.
Задача на эту сессию — ровно одна: G<НОМЕР> из ../docs/v2/tasks/integration.md.
Прочитай там карточку своей задачи целиком, включая раздел «Почему» —
он объясняет, какую ошибку задача обязана поймать.

Правила сессии:
- работаешь только в своих каталогах (карточка ../docs/v2/assignments/<имя>.md);
- contracts/ не правишь;
- типы импортируешь из contracts, не переизобретаешь;
- моков нет: не можешь посчитать — NotImplementedError, а не return 0.0;
- две шкалы времени не путаешь: StateAtDate живёт на 371 дате дека,
  IntervalResponse — на 224 интервалах управления;
- расписание для OpmDeckEmitter обязано быть плотным: статус и уставка
  у каждой скважины на каждом шаге, иначе дек не соберётся;
- validate_static физическую бессмыслицу не ловит — уставку сверяй с деком;
- приёмка — то, что написано в разделе «Приёмка» карточки.

Окружение:
- .venv уже собран, зависимости ставятся так: .venv/bin/pip install -e ".[dev,ml]"
  плюс отдельной командой anthropic==0.40.0 (в pyproject он не объявлен);
- тесты с настоящим OPM требуют запущенного Docker, без него корректно
  пропускаются; полный прогон при поднятом Docker — 1133 passed, 67 skipped;
- дек и эталонный расчётчик — ../docs/models/, переменная AIOS_DOCS_ROOT.

Данные лежат локально, считать заново ничего не нужно:
- базовый отклик OPM — aios/data/base_case/response.json;
- датасет 700 прогонов и checkpoint — ../dataset-700/;
- измеренная λ — aios/data/lambda-window-2007/lambda.json (108 прогонов,
  устойчивость 0.993), читается connectivity.measure.load_lambda;
- найденная θ* — aios/data/lambda-window-2007/cmaes.json, план
  восстанавливается из неё за 16 секунд, CMA-ES заново гонять не нужно;
- отклик нашего плана на настоящем OPM — aios/data/g7-submission/.

Готовые запускалки:
- python -m optimizer.search_run [бюджет] — поиск по θ;
- python -m bridge.submission_run — весь тракт сдачи на найденной θ*;
- python -m ui.plan_export — выгрузка плана в витрину;
- ./status.sh — статус-бар долгих прогонов.

Начни с прогона .venv/bin/pytest contracts/tests и скажи, что получилось.

Когда приёмка выполнена и код закоммичен — последним действием открой
../docs/v2/STATUS.md, поставь в колонку «Статус» своей строки значение
«готово ДД.ММ · <хеш коммита>» и впиши числа, которые задача просила
записать. Карточку задачи дополни разделом с результатом. Отдельным
коммитом в docs с сообщением «G<НОМЕР> закрыта». Если приёмка не выполнена —
ставь «в работе» и скажи, что осталось.
```

## Что запускать прямо сейчас

| Кто | Задача |
|---|---|
| Андрей | `G9`, затем `G11` и `G12`, затем `G10` |
| Михаил | очередь F с `F1`; `F3` и `F11` разблокированы измеренной λ |
| Иван | три вопроса трекеру, затем нарезка участков из λ |
| Савелий | данные витрины в образ, `anthropic` в `pyproject.toml`, задача 65 |

G9 стоит первой не по сложности, а потому что без неё остальные улучшают вслепую.
