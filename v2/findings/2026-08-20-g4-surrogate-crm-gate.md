# G4 — гейт суррогата против CRM и слияние ветки

Очередь G, задача G4. Владелец — Андрей.

**Что измерено.** Готовность гейта суррогата против CRM после слияния ветки (модель, обучение, цикл на 700 прогонов, дашборд — всё в `main`, включая `extras ml`) и качество обученной модели.

**Чем измерено.** Обучение прошло 18.08, разбор `surrogate/crm.py` после слияния.

**Число.** Обучение: 65 эпох, лучшая — 55-я, checkpoint `dataset-700/model-task34-700/model.pt`, `model_version` `0ba51419…`. Ранжирование слабое: Spearman `0.737` при precision@1 `0.0` и regret@1 `201` млн руб. Сравнивать модель с CRM негде: 105 сценариев, между которыми `surrogate/crm.py` не кросс-сценарный предиктор ЧДД.

**Вывод.** Сам гейт не собран. Вердикт «бьёт / не бьёт» не вынесен ни в какую сторону.

**Что дальше.** Вынесено отдельной задачей — см. [G8](2026-08-20-g8-crm-cross-scenario-adapter.md).
