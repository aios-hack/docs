# Тесты пропускались из-за путей чужой машины

Вне очередей. Владелец — интегратор. Исправлено 22.08 на ветке `refactor`.

**Что измерено.** Причина, по которой тесты связности и нормативов пропускались молча на каждом прогоне.

**Чем измерено.** Разбор `backend/domain/connectivity/tests/conftest.py` и `backend/domain/configuration/tests/test_normatives.py`.

**Число.** 64 теста связности и 1 тест нормативов пропускались молча — те же 65, что фигурировали в аудитных «65 skipped» как якобы свойство окружения.

**Вывод.** Дефолты держали путь чужой машины `W:/Projects/hacks/aios/docs/...`, поэтому на любой другой машине условие пропуска срабатывало всегда.

**Что дальше.** Оба файла перешли на резолвер корневого `conftest.py`; `AIOS_DECK_SCHEDULE` остался явным переопределением. Исправлено.
