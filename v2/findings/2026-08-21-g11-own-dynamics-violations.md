# G11 — семнадцать своих нарушений динамики

Очередь G, задача G11. Владелец — Андрей.

**Что измерено.** Адресация (скважина, устойчивость по кандидатам) семнадцати нарушений `validate_dynamic`, найденных в [G7](2026-08-20-g7-final-opm-run.md) на нашем собственном плане.

**Чем измерено.** Разбор нарушений по всем 40 кандидатам CMA-ES из [G10](2026-08-21-g10-verification-loop.md).

**Число.** `BHP_ABOVE_INJECTOR_LIMIT` — скважина 12, ровно одно нарушение, во всех 40 кандидатах. `NEGATIVE_INTERVAL_DELTA` держится за скважины 31, 4, 35, 19, 21. `BHP_LIMITED_WITHOUT_UNDERSHOOT` от θ зависит, адреса меняются.

**Вывод.** `BHP_ABOVE_INJECTOR_LIMIT` — устойчивый дефект одной скважины, не зависит от θ. Остальные два типа — частью устойчивые по адресу, частью зависят от параметров поиска.

**Что дальше.** Адресами по 21.08, дальнейшая правка не начата.
