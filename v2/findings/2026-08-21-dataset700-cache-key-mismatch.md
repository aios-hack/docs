# Датасет 700 прогонов невоспроизводим текущим кодом

Вне очередей. Не назначено. Блокирует любую работу по суррогату.

**Что измерено.** Совпадение ключа кеша прогонов (`deck_hash + canonical_schedule_hash + summary_hash`) между спецификациями, построенными сегодняшним `build_plan`, и спецификациями от 17.08, на которых собран датасет 700.

**Чем измерено.** Пересборка спецификации сценария `baseline` (без случайности) сегодняшним кодом и сравнение хешей с записанными 17.08.

**Число.** `plan_hash` 86c74787… против 8fbdd6bd…; у `baseline` `spec_hash` 721ca9be… против 240cafa8…. Все 699 записей кеша промахиваются по ключу.

**Вывод.** Разошёлся канон спецификации (вероятнее всего [G2](../tasks/integration.md), `f4a3cf8` — порядок оси скважин). Материализованные расписания не сохранены ни в `aios/data/`, ни в `../../dataset-700/` — деки удаляются `compact_artifacts=True`. Отклики (`SMSPEC`/`UNSMRY`) целы, но восстановить по ним пары «расписание → отклик» нечем.

**Что дальше.** Переобучение суррогата на 700 требует полной перегенерации — около 200 машино-часов OPM. Найдено 21.08.
