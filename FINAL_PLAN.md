# AIOS — план до финала 11.09.2026

Источник правды по работе команды до сдачи. Всё спорное правится здесь, а не решается в чате.
Написан 03.09.2026 по стенограмме чекпоинта 31.08 (`завод_сквад_транскрипция_ru.txt`),
постановке (`TASK.md`), журналу решений (`DISCUSSIONS.md`) и аудиту кода и данных
на `main` (коммит `4c6698f`) и ветке `jarvis` (`8238bba`) репозитория `aios`.

Файл лежит в репозитории `docs`. Пути к коду (`backend/…`, `frontend/…`, `config/…`, `tests/…`,
`out/…`, `data/…`) указаны относительно соседнего репозитория `aios/`; пути к материалам
организаторов (`models/…`, `TASK.md`, `checkpoints/…`) — относительно этого репозитория. Датасет
прогонов лежит в `../data/dataset-main` рядом с обоими.

Порядок приоритетов, если что-то не влезает: **1) замечания куратора с чекпоинта → 2) требования ТЗ и
процедуры проверки → 3) наши обещания на чекпоинте → 4) всё остальное.** Слова друга из плана
«трек 2 до 11 сентября» учтены только там, где подтверждены данными (§1.4).

---

## 0. Как работать по этому плану

### 0.1. Волны по три агента

Работа идёт волнами `W0 … W10`. В каждой волне параллельно работают **не больше трёх агентов**,
у каждого свой контур файлов (§10). Волна закрывается гейтом (§10, «Гейт волны»): полный прогон
тестов запускает только человек, агенты гоняют только тесты своего контура.

Три постоянных контура:

| Контур | Область | Владеет |
|---|---|---|
| **A — физика и проверка** | дек, PVT, компенсация, давление, геология, валидаторы, OPM-диагностика | `backend/domain/physics/**`, `backend/domain/geology/**`, `backend/domain/schedule/validate*.py`, `backend/infrastructure/opm/**`, `backend/core/contracts/constraints.py`, `config/**` |
| **B — агенты, суррогат, оптимизация** | иерархия агентов, правила, поиск, OPM-контур, суррогатная карточка, сдача | `backend/domain/policy/**`, `backend/ml/**`, `backend/application/**`, `backend/presentation/cli/**`, `backend/presentation/ui_export/**` |
| **C — интерфейс** | экраны, витрина данных, i18n, токены, Джарвис | `frontend/**` |

Документы (`*.md`) пишет тот контур, чей предмет; сводные документы (§9) — отдельный агент D в
волнах W7–W8 вместо одного из трёх.

Если задача трогает файл чужого контура — она разбивается на две с явной передачей (в описании
задачи есть строка «Передача: …»). Одновременно два агента один файл не правят.

### 0.2. Правила, которые не обсуждаются

- Перед правкой читать `ARCHITECTURE.md`, `backend/core/contracts/README.md`, этот файл.
- **Компонент, который не может посчитать результат, сообщает об ошибке, а не возвращает фиктивное
  значение.** Никаких `synthetic-demo` в витрине к 11.09 (§8.7).
- Производные величины (ЧДД, компенсация, давление) вычисляются из отклика, не хранятся рядом с
  расписанием как подпись.
- Направление зависимостей `presentation → application → domain → core`, `infrastructure` не
  решает бизнес-правила. `tests/architecture/test_layers.py` обязан оставаться зелёным.
- Новый код бэкенда: сообщения ошибок на русском (как во всём `backend/`), кроме `backend/application/jarvis/**`
  и `backend/presentation/api/**`, где уже принят английский.
- Фронт: ≤250 строк на файл, локальный CSS рядом с компонентом, цвета только через токены
  `src/theme/tokens.*.css`, строки только через словари `src/i18n/{ru,en}/*.json` (парность ключей
  проверяет `i18n.test.ts`), контраст держат тесты `tokens.test.ts`.
- В коде нет комментариев, импорты вверху файла.
- Числа по деку перепроверяются парсингом (`context/tools/check_deck_facts.py`), не
  переписываются из памяти.
- Ни один агент не делает `git stash`, `git commit`, `git push`, `git checkout -- .`. Ветки и
  коммиты — человек.
- Vitest запускается так: `npx --yes node@22.11.0 node_modules/vitest/vitest.mjs run <путь>`.
  pytest — из `aios/aios`: `python -m pytest <путь> -q`.
- Выполненная задача отмечается `[x]` в §10 сразу, а не в конце волны.
- Не менять данные организаторов в `models/**`. Диагностические деки (с RPTRST, FIPNUM,
  дополнительными SUMMARY-векторами) эмитируются в рабочие каталоги и никогда не сдаются.

### 0.3. Ежедневный ритм

- Утро: человек запускает полный прогон тестов, закрывает гейт прошлой волны, зеркалит `main` в GitLab
  организаторов (H-01), ставит три агента на новую волну.
- День: агенты работают в своих контурах; спорное пишется в этот файл, не решается в чате.
- Вечер: контур B ставит пакет OPM на ночь (§7.2); человек проверяет отчёты агентов по §0.4.
- Никакая волна не начинается, пока не закрыт гейт предыдущей; исключение — задачи документов D, они
  идут параллельно с W6.

### 0.4. Что считается «сделано»

У каждой задачи в §10 есть «Готово, когда». Без выполнения этого пункта задача не отмечается.
Слова «примерно», «должно работать», «проверю позже» в отчёте агента означают, что задача не
закрыта. Отчёт агента содержит: изменённые файлы, команду теста и её вывод (последние строки),
числа, если задача их производит.

---

## 1. Откуда взялся план

### 1.1. Что сказала куратор на чекпоинте 31.08 — дословно и что из этого следует

| Тайм-код | Цитата | Требование | Задачи |
|---|---|---|---|
| 00:11:34 | «в гидродинамическом моделировании ещё очень часто рисуют карты давления и насыщенности воды, в том числе их рисуют в динамике на весь период разработки» | Карты давления и водонасыщенности по слоям в динамике, синхронно с таймлайном | P-08, P-09, U-01, U-02 |
| 00:12:12 | «какую архитектуру используете, какие эксперименты проводили, какие метрики получены» | Письменная карточка суррогата, которую может озвучить любой участник; метрики на отложенных сценариях и на кандидатах оптимизатора | S-01…S-06, D-02 |
| 00:15:25 | «было бы здорово, если бы в эту архитектуру была заложена физика… перепроверьте то, что у вас нигде не нарушается физика» | Физические инварианты внутри суррогата и в проверке его прогнозов; отчёт о нарушениях физики по прогнозам | S-03, S-04, P-06 |
| 00:15:50 | «можете потом написать мне в чате» (про суррогат) | Сообщение куратору с архитектурой суррогата | H-03 |
| 00:16:21 | «какие агенты за что отвечают и какую роль выполняют в системе» | Реестр агентов с ролями, читаемый и в коде, и в интерфейсе, и на слайде | M-01…M-06, U-04, D-03 |
| 00:17:44 | «советую добавить отдельными слайдами в целом архитектуру вашего решения, в том числе архитектуру агентной системы, кто за что отвечает, потому что такие вопросы точно последуют» | Слайды «Архитектура решения», «Архитектура МАС», «Масштабируемость» | D-03, D-04, D-05 |
| 00:17:54 | «могут быть разломы. Учитываются ли они у вас… не ломаются ли они на этапе построения графа?» | Аудит геологии дека; барьеры на графе; поддержка `FAULTS`/`MULTFLT` в читателе для переноса на другие модели | P-10…P-14, U-05 |
| 00:18:53 | «Рёбра этого графа. Они каким образом считаются?» | Ответ в одну фразу с числами и ссылкой на код; объяснение λ в интерфейсе | D-02 (FAQ), U-06 |
| 00:19:32 | «настоятельно рекомендую ещё раз перепроверить геологию, каким образом она читается и учитывается. И всё-таки подумать, как можно добавить разломы… очень сильно будут влиять на фронт вытеснения… на это будут обращать внимание» | Геологический слой: EQLNUM-регионы, выклинивание (`PINCHREG`), `NTG`, `ACTNUM`, `MULTZ`; проверка, что измеренная связность не проходит через барьеры без подтверждения | P-10…P-14 |
| 00:20:30 | «ограничения… это ограничение на пластовое давление, ограничение по воде» | Проверка пластового давления (поле и регионы) и водного баланса как нарушений с видом и шагом | P-01…P-05, P-07 |
| 00:20:48 | «есть определённый коридор закачки, выше которого и ниже которого мы не можем выходить» | Коридор компенсации — главное ограничение; наш текущий кап «закачивай не больше добытого» неверен | P-01, P-02, P-03 |
| 00:20:57 | «на тестовом кейсе будет предоставлено некоторый объём жидкости в размере n… который вы можете закачивать дополнительно» | Параметр внешней воды `n` в конфигурации кейса, учитывается в проверке и в политике | P-03, O-03 |
| 00:21:17 | «масштабируемость… заложена ли… с точки зрения самой архитектуры и в идеале переноса на другое месторождение» | Две оси масштабируемости, каждая с доказательством: добавление агента; перенос на модель с разломами | M-05, M-06, P-14, D-05 |
| 00:21:51 | (мы) «не могу это никак доказать, потому что мы не предоставили просто даже архитектуру нашей системы всей» | Документ архитектуры и диаграмма потоков данных | D-01, D-03 |
| 00:23:02 | «Прикрутили ли вы LLM-ку… интерпретируемость в человекочитаемом виде… перепроверьте по требованиям» | LLM в ядре слоя интерпретируемости: Джарвис в `main`, объяснение решения на карточке скважины и в Совете | U-08, U-09, M-07 |
| 00:24:14 | «репозиторий… он у вас сейчас пустой?» | Заполнить GitLab организаторов, отметить куратора в общей беседе | H-01 |
| 00:24:44 | «можете скинуть в чат запись» | Отправить запись | H-02 |
| 00:26:20 | «обратите внимание на ограничения, которые представлены в тестовом кейсе» | Приём тестового кейса без ручной настройки: файл ограничений → проверка → поиск → сдача | O-03, O-04, O-06 |

Отдельное наблюдение из стенограммы: на три технических вопроса (суррогат, агенты, разломы) команда
ответила «этим занимался Андрей, он молчит». Это не проблема связи, а концентрация знания в одном
человеке. Поэтому §9 требует **FAQ на одну страницу на каждый вопрос**, который может зачитать любой,
и распределение вопросов по владельцам (§9.5).

### 1.2. Что мы пообещали на чекпоинте

- «перепроверить суррогат ещё раз» (00:25:09) → S-01…S-06;
- «добавить помощника… который будет доказывать, почему именно эта скважина откроется» (00:25:20) →
  Джарвис на ветке `jarvis` уже написан, нужно слить и подключить к решениям (U-08, U-09, M-07);
- «суррогат предлагает и ранжирует, ограничения отсекают, окончательное решение принимает OPM»
  (00:01:30) → контур O-01…O-05 обязан работать ровно так, а не остаться слайдом;
- «координатор месторождения видит общие лимиты добычи, закачки и воды; групповой уровень
  распределяет ресурс с учётом измеренной связности; исполнители на скважинах…» (00:03:10) →
  сейчас в витрине `hierarchy.json` это синтетика (`provenance: synthetic-demo`). Обязательно заменить
  настоящим `HierarchyTrace` (M-03, U-04).

### 1.3. Что требуют ТЗ и процедура проверки (из `TASK.md`)

- Сдаём: код, документация (назначение, архитектура, установка, вход/выход, параметры), инструкция
  запуска без ручной настройки, конфигурация для воспроизведения, Docker, `wells_schedule.inc` +
  заявленный ЧДД, интерфейс, описание суррогата (архитектура, обучение, метрики), слой
  интерпретируемости с LLM в ядре, отсутствие ручного вмешательства после запуска.
- Проверка: **один прогон ГГДМ, одна попытка**; расхождение заявленного и посчитанного ЧДД сверх
  «пары процентов» — дисквалификация. Значит, заявлять можно только число, полученное нашим OPM на
  ровно том расписании, которое сдаём (O-05).
- Критерии: корректность и воспроизводимость; учёт ограничений; техническое качество; эффективность
  (ЧДД против референса); обоснованность алгоритмов; презентация. Трек 2 дополнительно: эффективность
  вычислений, качество суррогата, масштабируемость до 100+ скважин.
- Антипаттерны организаторов: агент на скважину без координации; кормить сетке кубы; воспроизводить
  симулятор нейросетью; учить на запоминание; «разгонять всё до максимума» — ловится компенсацией.
- Тестовый кейс: ограничения + история; типы возмущений — остановки скважин, квоты, обводнённость,
  закачка, инфраструктура, техпараметры, ввод новых скважин, изменение фонда. Небольшое ограничение не
  должно превращать ошибку ЧДД из 10 % в 1000 %.

### 1.4. Что из плана друга подтверждено, что нет

| Пункт друга | Проверка по данным | Решение |
|---|---|---|
| Компенсация: убрать кап «вода = добытая», поставить коридор | Подтверждено: компенсация базового прогона 1.202…1.457 (медиана 1.275) на 224 шагах; наш коридор в `timeline.json.field_norms` 0.95…1.15; в `config/competition-constraints.json` 0.85…1.15 `diagnostic`; в `validate_dynamic.py` слово `compensation` не встречается; `ViolationKind` (33 вида) не содержит компенсации | Принято, расширено: компенсация считается в пластовых условиях (§4.1), коридор измеряется, а не назначается (P-02) |
| Давление: «FPR не читается» | Неверно: `base_run.py:62` запрашивает `FPR FOIP FWIP FOPT FWPT FWIT`, `compute_pressure_diagnostics` есть. Нет: региональных давлений, вида нарушения, участия в валидаторе | Принято частично (P-04, P-05, P-07) |
| Разломы: «не искать, их нет» | В деке нет `FAULTS`/`MULTFLT`, `MULTZ` = `547638*1`; но есть 5 регионов равновесия `EQLNUM` (1, 4, 13, 14, 15), `PINCHREG`, `NTG`, 306 974 неактивных ячеек из 547 638 | Отвергнуто «не отвечать». Куратор сказала «настоятельно рекомендую». Делаем геологический аудит и барьерный слой (§4.5) |
| LLM: импортов `infrastructure/llm` ноль | Верно для `main`; на ветке `jarvis` — полноценный модуль | Принято: слить `jarvis`, подключить к решениям |
| Слайды архитектуры и репозиторий | Куратор просила трижды | Поднято в первый приоритет (H-01, D-03…D-05) |

### 1.5. Что показал аудит кода и данных 03.09

Факты, на которых стоит план. Каждый проверен командой, а не памятью.

| Факт | Где | Следствие |
|---|---|---|
| Базовая компенсация 1.20…1.46 при коридоре 0.95…1.15 | `frontend/public/data/timeline.json` (`steps[*].field.compensation`, `field_norms`) | Коридор задан неверно, все 224 шага эталона «в нарушении» |
| `water_reinjection_fraction: 1.0`, `external_water_m3_per_day: 0.0` | `config/competition-constraints.json` | Политика связана капом, которого организаторы не вводили |
| `WATER_SAFETY_FACTOR = 0.85` | `backend/application/optimization/water_baseline_run.py:43` | Результат 7.714 млрд ₽ получен под несуществующим ограничением |
| `hierarchy.json`, `ablation.json` — `provenance: synthetic-demo` | `frontend/public/data/*.json`, `backend/presentation/ui_export/{hierarchy_view,ablation_view}.py` через `demo_rng.py` | Экран «Совет» показан куратору на синтетике |
| `whatif-injection-cut` — синтетический сценарий | `scenarios.json`, `demo.py` | В витрине нет ни одного проверенного OPM сценария кроме `base` |
| `data/base_case/response.json`, `data/lambda-window-2007/lambda.json` отсутствуют | `ls data` → только `base_run` | Поиск и верификация локально не запускаются; витрина построена из прогона `20260816T200926-8b4da543d1ed`, которого на диске нет |
| Локальные прогоны OPM 20.08 упали: Docker daemon не был запущен | `data/base_run/runs/*/flow.log` | Сейчас `docker images` показывает `openporousmedia/opmreleases:latest`; время прогона Model_Z на этой машине не измерено |
| Датасет 700 прогонов локально отсутствует | `../data/dataset-main` — 16 прогонов, из них часть упала на путях Windows (`too many colons`) | Метрики суррогата невоспроизводимы без данных Андрея |
| Машина: 16 логических ядер, 31 ГБ | `nproc`, `Win32_ComputerSystem` | Параллельно 3–4 прогона OPM |
| Дек: 91×102×59, активных ячеек 240 664, `EQLNUM` ∈ {1, 4, 13, 14, 15}, `SATNUM` ∈ {1, 2}, `FIP_ZONE` ∈ {0…5}, `FIPNUM` нет, `RPTSCHED 'RESTART=0'`, `RPTRST` нет, аквифер не объявлен | `models/Model_Z/*.inc` | Карты требуют своего диагностического дека с `RPTRST`; региональные давления требуют `FIPNUM` в диагностическом деке |
| PVT: Rs ≈ 0.0054 при Pb ≈ 1.13 бар, Bo ≈ 1.000, μo ≈ 34 сП, Bw = 1.0 | `Model_Z_props.inc` `PVTO`, `PVTW` | Нефть практически дегазированная и вязкая: поверхностная и пластовая компенсация для Model_Z отличаются < 1 %. Код всё равно считает в пластовых условиях — это условие переноса на Model_Y |
| Начальное давление 122–123 бар, BHP-пределы: добывающие 50 бар, нагнетательные 300 бар | `Model_Z_init.inc` `EQUIL`, `Model_Z_sch.inc` | Пол и потолок давления имеют физическое обоснование |
| 103 скважины, 371 дата, 225 управляющих дат, 224 интервала, 30 переводов под закачку (`'SHUT'` на добывающей стороне) | `WELSPECS`, `DATES`, `WCONPROD` | Контрактные инварианты не менялись |
| `ui/web` — 208 МБ устаревшего интерфейса, `tools/g10_*`, `to-delete/` | корень репозитория | Мусор в сдаваемом репозитории |
| Правило R7 (циклика) выключено по умолчанию | `backend/domain/policy/flags.py:27` | Заказчик: «циклику в обоих треках и нужно делать» |
| Одна группа `G1` на 103 скважины | `graph.json.groups` | Групповой уровень агентов фактически вырожден |
| Ветка `jarvis`: 217 файлов, +19 158 строк, не слита | `git diff --stat main jarvis` | LLM-критерий закрывается только после слияния |
| ESLint не настроен | `frontend/package.json` | Долг; решение — не добавлять до заморозки, полагаться на `tsc` и тесты |
| `backend/domain/configuration/tests/test_no_parameter_bypass.py` падает на константах `0.1` в `domain/connectivity/measure.py:65,71` | коммит `30b3692` | Починить в W0 (R-03) |
| `tests/architecture/test_layers.py` падает: `application/optimization/search_run.py` импортирует `backend.presentation.ui_export.scenarios` (`constraints_from_json`) | `search_run.py:65` | Перенести чтение кейса в `application` (R-09, совместить с O-03) |
| `tests/architecture/test_markdown_links.py` падает: `README.md:45` ссылается на отсутствующий `../dataset-700/README.md` | корень | Починить ссылку (R-02) |

---

## 2. Диагноз одним абзацем

Каркас проекта хороший: канонический формат расписания и хеши, эмиссия дека, запуск OPM, чтение
бинарного отклика, эталонная экономика, статический и динамический валидаторы, измеренная планом
эксперимента связность, иерархия правил, интерфейс с провенансом. Слабые места ровно те, на которые
указала куратор: **(1)** ограничения кейса заданы не физикой (кап по воде вместо коридора компенсации,
нет проверки пластового давления), **(2)** геология дека не читается дальше устьев и перфораций,
**(3)** агентная система есть в коде, но не видна ни в документе, ни в интерфейсе (витрина Совета —
синтетика), **(4)** суррогат не имеет карточки с архитектурой и метриками, которую может озвучить
любой, **(5)** нет сдаваемого расписания, проверенного OPM под правильными ограничениями, и нет
локальной воспроизводимости (базовый отклик и λ на диске отсутствуют).

---

## 3. Целевая картина к 11.09

К утру 11.09 в `main` лежит и воспроизводится одной командой:

1. `python -m backend.presentation.cli.run full --case config/cases/<кейс>.json` → каталог
   `out/runs/<id>/` со статусом `ready_to_submit`, `wells_schedule.inc`, заявленным ЧДД (равным
   OPM-ЧДД того же расписания) и `manifest.json` со всеми хешами (O-05).
2. Все ограничения кейса проверены динамическим валидатором с видами нарушений: коридор компенсации
   (поле и участки, пластовые условия), внешняя вода `n`, пол и потолок пластового давления (поле и
   регионы), BHP, лимиты кейса, простои. Отчёт лежит в `out/runs/<id>/validation/` (P-01…P-07).
3. Геологический аудит дека: барьеры (`EQLNUM`, выклинивание, `NTG`, неактивные ячейки, `MULT*`,
   `FAULTS`/`MULTFLT` если есть), их наложение на граф связности, список рёбер λ, пересекающих
   барьеры, с пометкой «подтверждено измерением / не подтверждено» (P-10…P-14, U-05).
4. Интерфейс: карты давления и водонасыщенности по слоям в динамике (U-01, U-02); экран «Совет» на
   настоящем `HierarchyTrace` с реестром агентов (U-04); редактор ограничений с типизированными полями
   коридора, `n`, давления (U-03); Trust с физическими признаками (U-07); Джарвис в `main` с
   объяснением решений (U-08, U-09); ни одного `synthetic-demo` (U-10).
5. Документы: `ARCHITECTURE.md` (обновлён), `ARCHITECTURE_MAS.md`, `PHYSICS.md`, `SURROGATE.md`,
   `SUBMISSION.md`, `FAQ.md` (D-01…D-06); слайды финала в `checkpoints/2026-09-11-*.md`
   (D-07); README с запуском в одну команду и Docker (D-08).
6. Репозиторий организаторов заполнен, куратор отмечена, запись отправлена, сообщение о суррогате
   написано (H-01…H-03).
7. Репозиторий чист: `ui/web`, `tools/g10_*`, `to-delete/` удалены или перенесены в архивный тег
   (R-05).

### 3.1. Критический путь и порядок резки

Если волны не закрываются по календарю, режем в этом порядке — снизу вверх. Верхний ярус не режется
ни при каких условиях.

| Ярус | Что | Почему | Задачи |
|---|---|---|---|
| **Без этого дисквалификация или ноль по критерию** | базовый отклик локально; кейс из файла; коридор компенсации и давление в валидаторе `hard`; incumbent, подтверждённый OPM; пакет сдачи с хешами и холодный повтор; README, ARCHITECTURE, ARCHITECTURE_MAS, FAQ, SUBMISSION; слайды; репозиторий организаторов | «один расчёт — одна попытка»; критерии «учёт ограничений», «техническое качество», «презентация»; куратор трижды просила архитектуру и репозиторий | P-00, R-01…R-03, R-09, P-01…P-03, P-05, O-01, O-03, O-05, O-07, O-09, D-01…D-03, D-07…D-09, H-01 |
| **Без этого теряем баллы, на которые указала куратор** | настоящий Совет с реестром агентов; геологический аудит и барьеры на графе; карточка суррогата с инвариантами; типизированные ограничения в интерфейсе; Trust с физикой; витрина без синтетики; Джарвис в `main` | вопросы 00:12, 00:16, 00:17, 00:19, 00:23 стенограммы; критерии трека 2 | M-01…M-04, M-09, P-04, P-06, P-07, P-10…P-12, S-01…S-03, U-03, U-04, U-07, U-10, U-08, U-09, M-07 |
| **Даёт отрыв** | карты давления и насыщенности; агенты давления и геологии через точку расширения; замер масштабирования; батарея устойчивости на OPM; второй пакет и циклика; априор и мини-дек с разломом | 00:11 «карты рисуют в динамике»; ось масштабируемости с доказательством; критерий «эффективность» | P-08, P-09, U-01, U-02, M-05, M-06, M-08, O-06, O-07b, M-10, P-13, P-14, P-15, S-04, S-06 |
| **Красиво, но режется первым** | резерв W9–W10, U-11…U-13, U-18, ESLint, абляция на OPM | не влияет на допуск и на ответы куратору | O-10, S-07, P-17, R-08, U-11…U-13, U-18 |

Правило резки: задача из нижнего яруса не начинается, пока в текущей волне есть незакрытая задача
верхних ярусов у того же контура.

---

## 4. Физика: принципы, формулы, где в коде

Всё в этом разделе — контракт для контура A. Формулы записаны так, чтобы их можно было прочитать
жюри с одного слайда и найти в коде по имени функции.

### 4.1. Компенсация отбора закачкой (voidage replacement)

Заказчик определил ловушку «разгонять всё до максимума» через компенсацию — **соотношение закачки и
отбора жидкости в пластовых условиях** (лекция 12.08, 01:02). Сейчас `timeline.py:158` и правило R5
считают поверхностное отношение `injection / production`. Переходим к пластовому:

```text
V_inj(k)  = Σ_i  q_wi(i,k) · B_w(p)                       закачанная вода, пластовые м³/сут
V_prod(k) = Σ_p [ q_o(p,k) · B_o(p) + q_w(p,k) · B_w(p) ]  отобранная жидкость, пластовые м³/сут
C(k)      = V_inj(k) / V_prod(k)
```

- `B_o(p)`, `B_w(p)` берутся из `PVTO`/`PVTW` дека (`backend/domain/physics/pvt.py`), давление —
  среднее пластовое региона или поля на шаге `k` (`FPR`/`RPR`). Свободного газа в Model_Z нет
  (Rs ≈ 0.0054 при Pb ≈ 1.13 бар), поэтому газовая часть отбора равна нулю; формула всё равно
  содержит член `q_g · B_g` с проверкой «Pb ниже текущего давления ⇒ член равен нулю», иначе ошибка —
  на Model_Y это условие может не выполниться.
- Для наших прогонов OPM истинное значение читается напрямую: `FVIR`/`FVPR` (пластовые объёмные
  дебиты) добавляются в диагностические SUMMARY-ключи рядом с `FPR` (`base_run.py:62`). Это
  единственный источник для проверки; PVT-пересчёт — для политики и суррогата, где отклика OPM ещё
  нет. Расхождение двух способов на базовом прогоне печатается в отчёте (P-02).
- Коридор `[C_min, C_max]` — параметр кейса `infrastructure.compensation_min/max` (уже есть в
  контракте `constraints.py`). Значения по умолчанию **не назначаются**: они измеряются по базовому
  прогону организаторов как `[min_k C_base(k) − δ, max_k C_base(k) + δ]` с `δ = 0.05` и записываются в
  `config/competition-constraints.json` с полем `compensation_basis: "reservoir"` и комментарием
  происхождения в `config/README.md`. Когда придёт тестовый кейс, коридор берётся из него (O-03).
- Область проверки: поле **и** участки (`compensation_scope: field_and_groups`). Участок без
  нагнетательных не проверяется, участок без отбора — ошибка данных, не «ноль».
- Виды нарушений: `COMPENSATION_BELOW_CORRIDOR`, `COMPENSATION_ABOVE_CORRIDOR` (значение — `C(k)`,
  деталь — область и коридор). Режим `compensation_enforcement: hard` делает их блокирующими;
  `diagnostic` — только отчёт. Для сдачи — `hard`.
- Правило R5 остаётся исполнителем коридора на уровне участка, но читает коридор из кейса, а не из
  собственных θ (`r5_compensation_low/high` становятся смещениями внутри коридора, не заменой).

### 4.2. Источник воды и объём `n`

Куратор: «коридор закачки… и на тестовом кейсе будет предоставлен объём жидкости `n`, который вы
можете закачивать дополнительно». Следствия:

- `water_reinjection_fraction` и `WATER_SAFETY_FACTOR` **удаляются из значений по умолчанию**. Поле
  остаётся в контракте как необязательное для кейсов, где организаторы явно ограничат источник.
- `external_water_m3_per_day` (уже есть) — это и есть `n`. Проверка: `Σ_i q_wi(k) ≤ Σ_p q_w(p,k) +
  n` на каждом шаге только если кейс объявил `water_reinjection_fraction`; иначе проверка не
  выполняется, а компенсация ограничена коридором §4.1. Вид нарушения `WATER_SUPPLY_LIMIT_EXCEEDED`
  уже есть.
- Политика: координатор поля получает лимит закачки как `min(лимит кейса, C_max · V_prod)` и пол
  `C_min · V_prod` (M-02).

### 4.3. Пластовое давление

- Поле: `FPR` уже читается. Добавляем регионы: `RPR` по `FIPNUM`. В деке `FIPNUM` нет, есть
  `FIP_ZONE` (0…5) и `FIP_C1`. Для диагностического дека контур A эмитирует `FIPNUM`, равный
  `FIP_ZONE` (нули → 1), в секцию REGIONS через дополнительный include; сдаваемое расписание это не
  трогает (P-04).
- Пол давления: `p_floor = max(Pb + Δ_b, p_lift)` где `Pb` из `PVTO`, `Δ_b = 5 бар`, `p_lift` — давление,
  при котором добывающие упираются в BHP 50 бар (диагностика `BHP_LIMITED_WITHOUT_UNDERSHOOT`
  показывает это раньше). Для Model_Z Pb ≈ 1.13 бар, поэтому фактический пол задаёт эксплуатация, а
  не разгазирование — это надо сказать на слайде, а не скрывать.
- Потолок давления: `p_ceiling = p_init · κ`, `κ = 1.25` по умолчанию; физический смысл — не
  раскрыть трещину (заказчик: «выше 300 атм в жизни раскрывается трещина»). Значение кейса имеет
  приоритет.
- Виды нарушений: `FIELD_PRESSURE_BELOW_FLOOR`, `FIELD_PRESSURE_ABOVE_CEILING`,
  `REGION_PRESSURE_BELOW_FLOOR`, `REGION_PRESSURE_ABOVE_CEILING`. Существующий
  `_PRESSURE_BLOWUP_FACTOR = 3.0` в `base_run.py` заменяется этими проверками.
- Давление доступно только после прогона OPM. Поэтому валидатор получает `FieldSeries | None`; если
  политика давления включена, а серии нет — `DynamicValidationError`, а не «пропуск» (инвариант
  CLAUDE.md). В поиске до OPM давление оценивается суррогатной головой `bhp` только как
  предупреждение (`SURROGATE_NONBLOCKING_KINDS` уже так устроен).

### 4.4. Материальный баланс и лаги

- Тождества `ΔFOIP + ΔFOPT ≈ 0`, `ΔFWIP − (ΔFWIT − ΔFWPT) ≈ 0` уже считаются (`base_run.py:241`).
  Переносятся в валидатор как `MATERIAL_BALANCE_BROKEN` с относительной ошибкой > 1 % (P-06).
- Лаг отклика на закачку измерен планом эксперимента (`estimator.scan_lag`, сетка 0…6 мес). Политика
  и суррогат обязаны использовать измеренный лаг, а не мгновенный отклик; это проверяет тест на
  `RuleContext.influence.lag_months`.

### 4.5. Геология и «разломы»

Ответ куратору строится на проверке, а не на отрицании:

1. **Что в деке.** `FAULTS` — 0, `MULTFLT` — 0, `MULTZ` — `547638*1`, `MULTX/MULTY` — нет. Зато:
   5 регионов равновесия `EQLNUM` с собственными `EQUIL`-строками; `PINCHREG` (выклинивание пластов);
   `NTG`; 56 % ячеек неактивны; два пласта (k 1–26 и 29–53) с промежутком k 27–28.
2. **Барьеры без разломов.** Гидродинамический барьер в Model_Z — это граница региона равновесия,
   выклинивание и полоса неактивных ячеек. Контур A строит **карту барьеров**
   (`backend/domain/geology/barriers.py`): для каждой пары соседних активных столбцов (i, j) флаг
   «разные `EQLNUM`», «нулевая `NTG`», «разрыв активности по k», «`MULT*` < 1», «`FAULTS`/`MULTFLT`
   пересекает грань».
3. **Наложение на граф связности.** Для каждого ребра λ (добывающая ← нагнетательная) считается
   `barrier_crossings` — число барьеров на отрезке между устьями с учётом слоя перфораций. Ребро с
   `λ` выше порога и `barrier_crossings > 0` попадает в отчёт «связь через барьер» и требует
   подтверждения: устойчивость между партиями плана (`stability_between`) и ненулевой лаг. Если
   подтверждения нет — ребро помечается `unverified` и групповой уровень его не использует (M-02).
4. **Геологический априор.** `backend/domain/geology/prior.py` считает для пары скважин
   гармоническую среднюю `PERMX·NTG` вдоль прямой между устьями по слоям перфораций и долю активных
   ячеек на пути. Это не связность (заказчик: «расстояние не означает гидродинамическую связь»), а
   ожидание: рёбра с большим λ и нулевым априором — первые кандидаты на проверку.
5. **Перенос.** Читатель геологии поддерживает `FAULTS`, `MULTFLT`, `MULTX/Y/Z`, `MULTREGT`,
   `NNC` синтаксически (юнит-тест на мини-деке с разломом), поэтому на Model_Y барьеры появятся без
   изменения кода. Это и есть доказательство оси «перенос на другое месторождение» (§5.5).
6. **В интерфейсе** барьеры рисуются слоем на экране «Поле» (U-05), рёбра через барьер получают
   пунктир и подпись «через барьер, подтверждено/не подтверждено».

### 4.6. Карты давления и водонасыщенности

- Диагностический дек получает `RPTRST BASIC=2` в SOLUTION (запись на каждый report step) — только в
  наших прогонах (`OpmDeckEmitter.emit(..., restart=True)`), сдаваемый дек не меняется.
- Читатель `backend/infrastructure/opm/restart_reader.py` разбирает `Model_Z.UNRST` тем же
  Fortran-record форматом, что `response_loader.py` (`SEQNUM`, `INTEHEAD`, `PRESSURE`, `SWAT`,
  `SGAS` при наличии) по активным ячейкам; `ACTNUM` даёт отображение в (i, j, k).
- Экспорт `backend/presentation/ui_export/field_maps.py`: для каждого пласта (k-диапазоны из
  `wells.json.layers`) и каждой из 225 управляющих дат — 2D-карта 91×102: давление как средневзвешенное
  по толщине, водонасыщенность — по поровому объёму (`PORO·NTG·объём ячейки`; объём из `ZCORN` можно
  заменить на `DZ` из `PORV`, если `EDIT/PORV` задан — он задан, 30 500 строк). Квантование в `uint8`
  по фиксированной шкале с записанными `min/max`; формат — один бинарный файл на свойство и слой
  (`225 × 9282` байт ≈ 2 МБ) плюс `maps.json` с метаданными и провенансом.
- Фронт (U-01, U-02): экран «Поле → Карты», выбор свойства и пласта, шкала цветов из токенов
  (монохромная по светлоте, чтобы читалась в обеих темах), устья и роли поверх карты, синхронизация с
  `TimelineContext.stepIndex`, воспроизведение плеером, всплывающая подсказка со значением в ячейке.
- Провенанс: карты существуют только для сценариев с настоящим прогоном OPM; для остальных экран
  честно показывает «карты не рассчитаны: нет прогона OPM».

### 4.7. Пределы на скважине

- BHP 50 бар (добывающие) и 300 бар (нагнетательные) уже в деке и валидаторе. Верхний предел уставки
  `MAX_LRAT_M3_PER_DAY = 500` — ограничение эталонного расчётчика, не физика; так и написано в
  контракте.
- Насос: типоразмер ЭЦН не убывает (храповик `WellMemory.with_esp`), смена типоразмера стоит денег —
  уже в экономике.
- Циклика (R7) включается в поиск как отдельная ветка кандидатов с обязательной OPM-проверкой; без
  подтверждённого прироста остаётся выключенной (O-02).

---

## 5. Архитектура мультиагентной системы

### 5.1. Зачем менять то, что есть

В коде уже есть три уровня (`Level.FIELD/GROUP/WELL` в `policy/hierarchy.py`), восемь правил R0–R7
с критериями допуска, неподвижная точка и журнал `HierarchyTrace`. Нет: **реестра агентов** как
объектов с ролями, **функциональных агентов** (вода, давление, геология, риск), **протокола**
предложений и арбитража, доказуемой **точки расширения** и **честного экспорта** в интерфейс.
Куратору нужно назвать агентов и их ответственность; жюри — увидеть, что добавить агента можно без
переписывания ядра.

### 5.2. Агенты и ответственность

```text
                     ┌──────────────────────────────────────────┐
                     │  Координатор поля (FieldCoordinator)      │
                     │  лимиты добычи/закачки/воды кейса,        │
                     │  коридор компенсации поля, распределение  │
                     │  лимита по участкам                       │
                     └───────────────┬──────────────────────────┘
        предложения ↑                │ квоты участков
                     │               ▼
   ┌──────────────┐  │  ┌──────────────────────────────┐   ┌──────────────────────────┐
   │ Агент воды и │──┤  │ Распорядитель участка ×G     │   │ Агент давления           │
   │ компенсации  │  │  │ (GroupAllocator)             │   │ (PressureAgent)          │
   │ V_inj/V_prod │  │  │ делит квоту между своими     │   │ пол/потолок FPR, RPR,    │
   │ n, коридор   │  │  │ скважинами по измеренной λ   │   │ BHP-срывы, предупреждения│
   └──────────────┘  │  └──────────────┬───────────────┘   └──────────────────────────┘
   ┌──────────────┐  │                 │ уставки              ┌──────────────────────────┐
   │ Агент        │──┤                 ▼                      │ Агент геологии           │
   │ экономики    │  │  ┌──────────────────────────────┐      │ (GeologyAgent)           │
   │ маржа, ЭЦН,  │  │  │ Исполнитель скважины ×103    │      │ барьеры, рёбра через     │
   │ окупаемость  │  │  │ (WellExecutor) R0…R7         │      │ барьер, априор           │
   └──────────────┘  │  └──────────────────────────────┘      └──────────────────────────┘
   ┌──────────────┐  │
   │ Агент риска и│──┘   ┌──────────────────────────────┐   ┌──────────────────────────┐
   │ верификации  │      │ Проекция на жёсткие          │   │ Объясняющий агент (LLM)  │
   │ OOD, регрет, │      │ ограничения → суррогат →     │   │ Джарвис: журнал фактов → │
   │ soundness    │      │ OPM → sound?                 │   │ текст/сцена, без выбора  │
   └──────────────┘      └──────────────────────────────┘   │ чисел                    │
                                                            └──────────────────────────┘
```

| Агент | Уровень | Читает | Пишет | Где в коде (цель) |
|---|---|---|---|---|
| FieldCoordinator | FIELD | ограничения кейса, `V_prod`, коридор, `n` | квоты закачки/отбора по участкам | `policy/agents/field.py` (из `hierarchy.py`) |
| GroupAllocator | GROUP | квота участка, λ (только `verified` рёбра), роли | уставки-цели по скважинам участка | `policy/agents/group.py` |
| WellExecutor | WELL | наблюдение скважины, память (R3, R4, R6), θ | `ControlEvent` через правила R0…R7 | `policy/agents/well.py` + `rules/*` |
| WaterAgent | FIELD/GROUP | `q_w`, `q_wi`, PVT, `n` | компенсация `C(k)`, предупреждения коридора | `policy/agents/water.py` → `domain/physics/voidage.py` |
| PressureAgent | FIELD/GROUP | `FPR`, `RPR`, BHP-диагностика | предупреждения пола/потолка, снижение квоты | `policy/agents/pressure.py` |
| GeologyAgent | GROUP | карта барьеров, λ | пометки `verified/unverified` на рёбрах, запрет использования непроверенных | `policy/agents/geology.py` → `domain/geology/**` |
| EconomicsAgent | WELL | нормативы, ЭЦН, маржа | `marginal_value_rub_per_m3`, окупаемость перевода | существующие R1/R4/R6 как функции агента |
| RiskAgent | FIELD | OOD, регрет батареи, отчёт валидатора | вердикт «в OPM / отклонить», статус sound | `application/optimization/verification.py` |
| ExplainerAgent | — | `HierarchyTrace`, факты, витрина | текст и сцены; **не выбирает числа** | `application/jarvis/**` |

Правило протокола: агенты **предлагают**, проекция на ограничения **отсекает**, OPM **решает**. Ни один
агент не имеет права записать уставку в обход `project_to_hard_constraints` (тест M-04).

### 5.3. Реестр и точка расширения

```python
class Agent(Protocol):
    name: str
    level: Level
    responsibilities: tuple[str, ...]
    def propose(self, state: PolicyState, context: RuleContext) -> Proposal: ...
```

`policy/agents/registry.py` хранит кортеж агентов и порядок их вызова на шаге; `HierarchyTrace`
получает поле `agent` из реестра (оно уже есть в `LeveledTraceEntry.agent`). Добавление агента —
новый файл, реализующий `Agent`, плюс строка в реестре; архитектурный тест проверяет, что все агенты
реестра покрыты юнит-тестом и описаны в `ARCHITECTURE_MAS.md` (таблица генерируется из реестра, не
пишется руками — M-06).

Доказательство оси «добавить агента»: `PressureAgent` и `GeologyAgent` пишутся именно через эту
точку расширения, и в отчёте волны фиксируется, какие файлы ядра при этом **не** менялись.

### 5.4. Групповой уровень, который не вырожден

Сейчас одна группа `G1`. Причины — порог слияния `MERGE_OVERLAP_DEFAULT = 0.5` и коэффициенты λ с
одним лагом. Что делаем:

- пересчитываем группы на измеренной λ с фильтром `verified` (после P-12) и печатаем
  `GroupingReport` с числом участков, изолированных добывающих, двойных ролей;
- если участков всё равно один — так и говорим на слайде («связность Model_Z не даёт устойчивого
  разбиения; групповой уровень работает как один участок, архитектура при этом не меняется»), но
  показываем тот же алгоритм на синтетическом мини-поле из двух изолированных блоков (тест) — это
  честнее, чем нарисованная иерархия.

### 5.5. Масштабируемость — две оси и доказательства

| Ось | Что показываем | Как доказываем |
|---|---|---|
| Архитектурная (новые агенты, новые правила) | реестр агентов, протокол `Agent`, флаги правил | M-05: добавление `PressureAgent` без правок ядра, список нетронутых файлов; тест реестра |
| Фонд 100+ скважин | сложность шага O(скважины + рёбра λ) | M-08: замер времени шага политики на 103, 206, 412 скважинах (дублирование фонда в тесте), таблица в `ARCHITECTURE_MAS.md` |
| Новое месторождение | ни одного литерала скважины/даты в `domain/**` и `application/**`; читатель геологии с `FAULTS` | тест `test_no_parameter_bypass` расширить на идентификаторы скважин; P-14 мини-дек с разломом; `config/` полностью описывает кейс |
| Вычислительные ресурсы (трек 2) | суррогат ранжирует за секунды, OPM только для шортлиста | O-04: журнал бюджета — сколько кандидатов оценено суррогатом, сколько ушло в OPM, время |

---

## 6. Суррогатная модель

### 6.1. Роль

Суррогат **не судья и не второй симулятор**. Он: (1) ранжирует кандидатов расписания по ожидаемому
`ΔЧДД` относительно проверенного incumbent, (2) возвращает признак выхода из области обучения (OOD),
(3) оценивает риск динамических нарушений до OPM. Финальное число даёт только OPM. Это уже сказано на
чекпоинте; здесь фиксируется, что именно должно быть написано и измерено.

### 6.2. Карточка суррогата (`SURROGATE.md`, S-01)

Разделы, каждый с числами и ссылкой на код:

1. **Вход.** `SurrogateInput` из `ScheduleFeatureizer` (`ml/surrogate/features.py`): накопленные цели
   по скважине, измеренные агрегаты λ, роль, статус, вложение скважины; никаких величин из отклика
   симулятора на инференсе.
2. **Архитектура.** Полнотраекторная модель по узлам `(скважина, шаг)` с шестью каналами
   (`TARGET_NAMES` в `model.py`), параметризация `watercut` (нефть = жидкость × (1 − обводнённость)),
   ансамбль (`ensemble.py`), экономическая голова (`npv_head.py`), CRM как обязательная базовая линия
   (`crm.py`). Размеры, число параметров, оптимизатор, эпохи — из чекпойнта, не по памяти.
3. **Обучение.** Датасет: сколько независимых расписаний (698/700), семейства возмущений (`LEVELS`,
   `UNREACHABLE`, `SHUTDOWN`, `CONVERSION`), сплит по сценариям (не по узлам), ранговый член лосса,
   калибровка.
4. **Метрики.** Три таблицы: (а) отложенные сценарии знакомого распределения — Spearman, R², MAE,
   ошибка ЧДД в %; (б) кандидаты оптимизатора (40 из G10) — Spearman, место истинного лучшего;
   (в) сравнение с CRM-базой. Числа с чекпоинта (0.0036 → 3–6 место из 40; 0.52 % ЧДД; шум ÷2.29)
   воспроизводятся скриптом `S-02`, иначе не публикуются.
5. **Физические инварианты** (§6.3) и результат их проверки на прогнозах.
6. **Границы.** Что модель не умеет: новые классы ограничений, новая история → OOD → abstain.

### 6.3. Физические инварианты прогноза (S-03, S-04)

Проверяются на каждом прогнозе (`ml/surrogate/physics_checks.py`), результат идёт в Trust:

| Инвариант | Проверка |
|---|---|
| Неотрицательность | все дебиты, приёмистости, накопленные объёмы ≥ 0 |
| Обводнённость | `0 ≤ wc ≤ 1`, нефть = жидкость × (1 − wc) — тождество параметризации |
| Монотонность накопленных | `liquid_volume_delta ≥ 0` на каждом интервале |
| Отклик на закачку | рост закачки нагнетательной не уменьшает суммарную жидкость связанных добывающих в пределах лага (знак λ) |
| Материальный баланс | Σ прироста жидкости соседей ≤ закачано (условие CRM `Σ_p f[p][i] ≤ 1`) |
| Давление | голова `bhp` добывающих ≥ 50 бар, нагнетательных ≤ 300 бар; иначе кандидат в предупреждениях |
| Закрытая скважина | прогноз дебита закрытой скважины равен нулю |

Нарушение инварианта — не «штраф», а флаг кандидата: он не идёт в OPM, пока флаг не снят.

### 6.4. Датасет и воспроизводимость (S-05)

Локально 16 прогонов. Датасет 700 у Андрея. Задача человека H-04: получить каталог откликов
(`response.json` на сценарий, ~700 файлов) и положить в `../data/dataset-main/runs` или
восстановить из кеша. Без него метрики §6.2 (4) не воспроизводимы, и карточка публикуется с
пометкой «числа с 31.08, повтор невозможен» — это плохой исход, его надо избежать до W3.

Ошибка Windows-путей в `DatasetGenerator` (`\\?\W:\… too many colons`) чинится в W0 (R-04), иначе
новые прогоны на этой машине не соберутся.

---

## 7. Контур оптимизации и сдачи

### 7.1. Цикл

```text
кейс (ограничения + история)
  → канонический incumbent: базовое расписание организаторов, спроецированное на ограничения кейса
  → OPM incumbent (истина, sound?)
  → пакет локальных возмущений компактной политики:
       уровни отбора ±{2.5, 5}%, уровни закачки ±{1.25, 2.5, 5}%, перераспределение закачки
       к/от скважин с наибольшим λ, ветка циклики, ветка переводов
  → проекция на жёсткие ограничения (коридор, n, лимиты кейса, BHP-пределы, простои)
  → суррогат: ΔЧДД, OOD, физические флаги → консервативный отбор (ΔЧДД − β·σ, OOD ≤ порог, флагов нет)
  → 3–4 кандидата параллельно в OPM
  → динамический валидатор с давлением и компенсацией → sound
  → новый incumbent только при sound и фактическом ЧДД > incumbent + ε
  → сужение области при отсутствии улучшения; стоп по бюджету
  → сдача: incumbent, его `wells_schedule.inc`, его OPM-ЧДД, манифест
```

Это уже записано в `DISCUSSIONS.md` (24.08) и подтверждено десятью OPM-прогонами (`+5 % отбор /
+2.5 % закачка` дало +97 млн ₽ к якорю). Задача — сделать это командой, а не ручным скриптом.

### 7.2. Бюджет OPM

Время одного прогона Model_Z на этой машине неизвестно — W0 измеряет (P-00). Оценка: 10–25 мин, 3–4
параллельно (31 ГБ). План расходов:

| Волна | Прогоны | Зачем |
|---|---:|---|
| W0 | 1 + 2 | базовый отклик (`data/base_case/response.json`), повтор для детерминизма, замер времени |
| W1 | 4 | базовый с диагностическим деком (`RPTRST`, `FIPNUM`, `FVIR/FVPR`), проверка компенсации и давления |
| W2 | 8 | план эксперимента для λ на окне 2007 (если `lambda.json` не восстановлен из архива) |
| W4 | 12 | первый пакет trust-region |
| W5 | 12 | второй пакет + ветка циклики |
| W6 | 8 | сценарии батареи устойчивости (для витрины и слайда «ошибка не растёт в 100 раз») |
| W8 | 3 | холодный повтор сдаваемого расписания (из чистого клона, из Docker) |

Итого ≈ 50 прогонов, около двух суток машинного времени при параллелизме 3. Журнал бюджета пишет
`out/opm-budget.jsonl` (O-04).

### 7.3. Приём тестового кейса без ручной настройки (O-03)

`config/cases/<имя>.json` в формате `Constraints` (годовые лимиты, простои, `infrastructure` с
коридором, `n`, полом/потолком давления). CLI: `run full --case <файл>`. Валидация файла — ошибка с
указанием поля; неизвестное поле — ошибка, не игнор. Редактор ограничений в интерфейсе (U-03)
экспортирует ровно этот формат, и импорт тестового кейса в интерфейс — это чтение того же файла.

Типы возмущений ТЗ ↔ поля кейса: остановка скважин → `well_outages`; квота ОПЕК (потолок по нефти) →
`oil_limits` (новое поле, год → т/сут, P-07); цель по добыче → `production_floors`; лимит жидкости →
`liquid_limits`; обводнённость → `watercut_limits`; закачка → `injection_limits`; инфраструктура
→ `infrastructure.*`; техпараметры → BHP-пределы в `infrastructure.bhp_*` (новое, P-07); плановый ввод скважин → `commissioning_shifts`
(скважина → новая дата ввода) для 22 скважин, которые вводятся по истории после 01.01.2007: сейчас
валидатор запрещает это видом `FIXED_LAYER_CHANGED`, поэтому сначала оценка (O-11), затем реализация
(O-12) — перенос блоков `COMPDAT`/`WCONPROD` в эмиссии; бурение новых скважин, которых нет в деке →
**не поддерживается** и явно объявляется ошибкой кейса с текстом причины (поле `new_wells` → отказ с
объяснением). Это честнее, чем молча проигнорировать.

### 7.4. Пакет сдачи (O-05, `SUBMISSION.md`)

`out/runs/<id>/submission/`: `wells_schedule.inc` (эмиссия из канонического расписания, round-trip
проверен), `claimed_npv.json` (ЧДД OPM того же расписания, хеш расписания, хеш дека, хеш нормативов,
версия образа OPM), `validation/` (все отчёты), `provenance.json` (кейс, seed, коммит). Скрипт
`selfcheck` перечитывает `wells_schedule.inc`, канонизирует и сравнивает хеш с манифестом — иначе
`ready_to_submit` не ставится.

---

## 8. Интерфейс

Правила фронта (§0.2) действуют без исключений. Ниже — что появляется и что меняется. Каждый новый
экран получает: словари `ru/en`, локальный CSS, тест на рендер с настоящей формой данных из
`src/data/validators.ts`, якорь `data-guide` для Джарвиса, состояние «данных нет» с причиной.

### 8.1. «Поле → Карты» (U-01, U-02)

Новый вид `field/maps` рядом с `field/projection`. Canvas 91×102 с квантованными `uint8`-картами из
`public/data/<сценарий>/maps/{pressure,swat}-layer{1,2}.bin` и `maps.json`. Управление: свойство
(давление / водонасыщенность / нефтенасыщенность = 1 − Sw), пласт (1 / 2), шаг — из `TimelineContext`,
плеер работает. Поверх — устья с ролью и статусом на шаге, выбранная скважина подсвечена (общий
`SelectionRings`). Легенда — шкала из токенов `--color-map-*` (новые, светлотная монохромная шкала,
проверяется `tokens.test.ts` на контраст подписей). Подсказка по ячейке: значение с единицей, `i, j`.
Без прогона OPM — `ViewStatus` «карты не рассчитаны для сценария …».

### 8.2. «Поле → Проекция»: барьеры и объяснение λ (U-05, U-06)

Слой барьеров (`BarrierLayer.tsx`) из `geology.json`: границы `EQLNUM`, зоны выклинивания, полосы
неактивных ячеек — тонкие линии из токена `--color-barrier`. Рёбра λ с `barrier_crossings > 0`
рисуются пунктиром; в инспекторе ребра — «через барьер: подтверждено измерением (устойчивость 0.53,
лаг 2 мес) / не подтверждено». `InfoHint` над графом: «Как посчитаны рёбра» — три строки из
`graph.json.meta` (план эксперимента, лаг, устойчивость между партиями, ранг, число обусловленности).

### 8.3. «Решения → Ограничения» (U-03)

Редактор кейса получает типизированные поля вместо свободных пар в `infrastructure`: коридор
компенсации (`min`, `max`, основа `reservoir/surface`, режим `hard/diagnostic`, область), внешняя
вода `n`, пол и потолок давления (поле и регионы), BHP-пределы. Валидация повторяет
`compensation_policy()` бэкенда (пустой коридор, отрицательные значения, `min ≤ max`). Импорт/экспорт
файла кейса `config/cases/*.json` — тот же формат, что читает CLI (O-03). Сравнение сценариев показывает
разницу по этим полям.

### 8.4. «Решения → Совет» на настоящих данных (U-04)

`hierarchy.json` строится из `HierarchyTrace` (M-03), провенанс — прогон. Экран показывает на шаге:
квоты координатора по участкам, распределение участка по скважинам, решение исполнителя с правилом и
физическим ограничением, **и новую панель «Агенты»**: реестр (имя, уровень, ответственность), какие
агенты сработали на шаге, сколько предложений отсечено проекцией на ограничения. Строки таблицы
кликабельны в карточку скважины. Если для сценария журнала нет — экран так и говорит.

### 8.5. Trust: физические признаки (U-07)

Новые индикаторы в `TrustBoard/indicators.ts`: компенсация поля в пластовых условиях против коридора
(значение, статус), давление поля и регионов против пола/потолка, материальный баланс (относительная
ошибка), геологический аудит (рёбер через барьер: подтверждено / не подтверждено), физические
инварианты суррогата (сколько флагов на кандидате), OPM-подтверждение (sound, хеши совпадают). Каждый
индикатор имеет три состояния: измерено-в норме, измерено-нарушение, не измерено — как сейчас.

### 8.6. Обзор и деньги (U-11, U-12)

Обзор: у карточек появляются единицы (долг), полоса компенсации по шагам с коридором, давление поля.
Деньги: сравнение incumbent с базой организаторов и с референсным ЧДД, если он известен; подпись
«подтверждено OPM, прогон …» у каждого числа.

### 8.7. Провенанс без синтетики (U-10)

К 11.09 в `public/data` нет артефактов с `provenance: synthetic-demo`. Сценарий
`whatif-injection-cut` заменяется реальным сценарием батареи устойчивости с прогоном OPM (W6), либо
удаляется. `ablation.json`: либо измерен OPM-прогонами с выключенными правилами (2–3 правила, бюджет
W6), либо экспортируется с `delta_npv: null` и статусом «не измерено» — интерфейс это умеет.
Валидаторы `src/data/validators.ts` получают проверку «`synthetic: true` запрещён в production-сборке»
(тест).

### 8.8. Джарвис (U-08, U-09)

Слить ветку `jarvis` в `main` (человек, после проверки R-06). Подключить к решениям: карточка
скважины и строка Совета получают действие «Объяснить» → сцена Джарвиса с фактами журнала (`explain_decision`
через `application/jarvis/tools`). База знаний пополняется статьями: компенсация в пластовых
условиях, коридор, внешняя вода `n`, пластовое давление и пол/потолок, барьеры и `EQLNUM`, как
измерена λ, что такое sound. Сторож провенанса чисел (§7 `JARVIS.md`) обязателен для новых карточек.
Без `OPENROUTER_API_KEY` интерфейс показывает честный статус, не пустоту.

### 8.9. Долги интерфейса, которые закрываем попутно

- Комментарий Кая на демо «граф выглядит не очень хорошо»: порог рёбер по умолчанию и толщина
  подбираются так, чтобы на 103 скважинах читались ≤ 400 рёбер (U-13).
- Заголовки вкладок и фавикон уже сделаны; проверить на новых видах (`documentTitle.ts`).
- ESLint не добавляем до заморозки; `tsc --noEmit` и тесты — обязательный гейт.

---

## 9. Документация, презентация, организационное

### 9.1. Документы в репозитории `aios`

| Файл | Содержание | Кто | Задача |
|---|---|---|---|
| `ARCHITECTURE.md` | обновить дерево (`domain/physics`, `domain/geology`, `policy/agents`, `presentation/api`), диаграмму потоков, команды запуска | D | D-01 |
| `ARCHITECTURE_MAS.md` | агенты и роли (таблица генерируется из реестра), протокол, уровни, точка расширения, две оси масштабируемости с доказательствами и замерами | B → D | D-03 |
| `PHYSICS.md` | §4 этого плана в виде справочника с формулами, источниками (лекции, дек), именами функций и видов нарушений | A → D | D-06 |
| `SURROGATE.md` | карточка §6.2 с таблицами метрик и командой воспроизведения | B | S-01 |
| `SUBMISSION.md` | как получить сдаваемый пакет одной командой, что в нём, как проверить хеши, что заявляем | B → D | D-09 |
| `FAQ.md` | ответы на вопросы куратора и ожидаемые вопросы жюри — одна страница на вопрос, с числами и путями к коду | D | D-02 |
| `README.md` | запуск в одну команду (локально и Docker), состав кейса, где результаты | D | D-08 |
| `config/README.md` | каждый параметр кейса: смысл, единицы, откуда значение по умолчанию | A | P-03 |

`tests/architecture/test_markdown_links.py` проверяет ссылки в документах `aios` — все относительные
пути должны существовать. Этот план лежит в `docs` и под проверку не попадает; при переносе фрагментов
в документы `aios` пути перепроверять.

### 9.2. Слайды финала (`checkpoints/2026-09-11-final-pitch.md`, D-07)

Семь минут, порядок:

1. Задача и цепочка решения одной картинкой: кейс → агенты → проекция → суррогат → OPM → sound → сдача.
2. **Архитектура решения** (то, что просила куратор): слои, потоки данных, где OPM, где LLM.
3. **Архитектура МАС**: девять агентов, кто за что отвечает, протокол «предлагают — отсекают —
   решает OPM».
4. **Физика**: компенсация в пластовых условиях и коридор, давление пол/потолок, материальный
   баланс, барьеры вместо разломов — по одной формуле и одному числу с базового прогона.
5. **Суррогат**: архитектура, обучение, три таблицы метрик, инварианты, роль ранжировщика.
6. **Масштабируемость**: две оси с доказательствами (добавленный агент, мини-дек с разломом, замер
   на 412 скважинах).
7. **Результат**: incumbent против базы и референса, sound, хеши, холодный повтор.
8. **Демо** (2 мин): Карты → Поле с барьерами → Совет с агентами → Trust → Джарвис объясняет решение.
9. Что не сделали и почему (одна строка на пункт).

### 9.3. Сообщение куратору о суррогате (H-03)

Пишется после S-01 из карточки: 8–10 строк — вход, архитектура, датасет, три числа метрик, роль в
контуре, инварианты. Отправляет Андрей или Кай; текст готовит B.

### 9.4. Репозиторий организаторов (H-01)

До конца W1: зеркало `main` в GitLab организаторов (без `ui/web`, `to-delete`, без данных
организаторов — они монтируются), README читается с первого экрана, тег куратора в общей беседе с
ссылкой на коммит. Далее — зеркалить в конце каждой волны.

### 9.5. Владельцы вопросов на защите

| Тема | Владелец | Дублёр | Материал |
|---|---|---|---|
| Суррогат: архитектура, метрики, OOD | Андрей | Кай | `SURROGATE.md`, `FAQ.md` §1 |
| Агенты и роли, масштабируемость | Савелий | Михаил | `ARCHITECTURE_MAS.md`, `FAQ.md` §2–3 |
| Физика: компенсация, давление, вода, барьеры | Иван | Андрей | `PHYSICS.md`, `FAQ.md` §4–6 |
| Как посчитаны рёбра λ | Иван | Савелий | `FAQ.md` §5 |
| Интерфейс, провенанс, LLM | Михаил | Савелий | `FAQ.md` §7, демо-маршрут |
| Сдача, воспроизводимость, хеши | Кай | Иван | `SUBMISSION.md`, `FAQ.md` §8 |

Правило чекпоинта повторяется: на вопрос сначала одна фраза, потом доказательство; дублёр отвечает,
если владелец молчит дольше пяти секунд.

---

## 10. Волны и задачи (TODO)

Обозначения: **A / B / C** — контуры §0.1, **D** — документы (заменяет один контур в W7–W8),
**H** — человек. У каждой задачи: файлы, что сделать, «Готово, когда», тест. Отметка `[x]` ставится
сразу по закрытии. Даты — ориентир; волна закрывается гейтом, а не календарём.

### W0 — фундамент воспроизводимости (03.09, вечер)

Цель: на этой машине снова можно прогнать OPM, есть базовый отклик, зелёные тесты.

- [ ] **P-00 · Базовый прогон OPM и замер времени** — A.
  Файлы: `backend/infrastructure/opm/base_run.py`, новый `backend/presentation/cli/base_run.py`,
  `data/base_case/response.json`, `data/base_case/field_series.json`.
  Сделать: запустить базовый прогон через `CachingOpmRunner` с диагностическими ключами
  (`FPR FOIP FWIP FOPT FWPT FWIT` + `FVIR FVPR FVIT FVPT`), сохранить `ResponseArtifact` и полевые
  серии, записать `wallclock_seconds`, повторить прогон и сравнить хеш отклика.
  Готово, когда: `data/base_case/response.json` существует, `response_hash` совпадает с
  `frontend/public/data/timeline.json.meta.response_hash` либо расхождение объяснено в отчёте;
  время прогона записано в `out/opm-budget.jsonl`; два прогона дают одинаковый хеш.
  Тест: `backend/infrastructure/opm/tests/test_base_run.py` (skip без Docker).
- [ ] **R-01 · Пути Windows в запуске Docker** — A.
  Файлы: `backend/infrastructure/opm/runner.py`, `backend/infrastructure/opm/dataset.py`.
  Сделать: монтирование каталогов через `Path.resolve()` без префикса `\\?\`, форма `-v host:cont`
  проверяется тестом на путях с буквой диска.
  Готово, когда: `python build_dataset.py` на одном сценарии не падает с `too many colons`.
  Тест: `backend/infrastructure/opm/tests/test_runner.py` — кейс с путём `W:\...`.
- [ ] **R-02 · Битая ссылка в `README.md`** — B.
  Файлы: `README.md`.
  Сделать: ссылку на `../dataset-700/README.md` заменить описанием фактического расположения датасета
  (`../data/dataset-main`, S-05) либо добавить в `EXPLICIT_EXCLUSIONS` теста с обоснованием.
  Готово, когда: `python -m pytest tests/architecture/test_markdown_links.py -q` зелёный.
- [ ] **R-09 · Нарушение слоёв в `search_run.py`** — B.
  Файлы: `backend/application/optimization/search_run.py`, `backend/application/optimization/verification_run.py`,
  `backend/presentation/ui_export/scenarios.py`, новый `backend/application/cases.py` (заготовка для O-03).
  Сделать: `constraints_from_json` переезжает в `application/cases.py`; `ui_export/scenarios.py`
  импортирует его оттуда, а не наоборот.
  Готово, когда: `python -m pytest tests/architecture/test_layers.py -q` зелёный, тесты `ui_export` зелёные.
- [ ] **R-03 · Починить `test_no_parameter_bypass`** — B.
  Файлы: `backend/domain/connectivity/measure.py`, `backend/domain/configuration/schema.py`.
  Сделать: константы `0.1` (допуск недобора и порог разделения) вынести в параметры конфигурации с
  именами и документацией.
  Готово, когда: `python -m pytest backend/domain/configuration -q` зелёный.
- [ ] **O-08 · Восстановить λ** — B.
  Файлы: `data/lambda-window-2007/lambda.json`, `backend/application/connectivity_measure.py`.
  Сделать: найти артефакт в архивном теге / у команды (H-04); если нет — подготовить план кампании
  (8 прогонов, окно 2007, две партии) к запуску в W2 и записать в `out/opm-budget.jsonl`.
  Готово, когда: `lambda.json` лежит на диске с провенансом, либо план кампании готов к запуску одной
  командой.
- [ ] **S-05 · Инвентаризация датасета** — B.
  Файлы: `../data/dataset-main/manifest.jsonl`, `backend/infrastructure/opm/dataset.py`.
  Сделать: скрипт-отчёт: сколько сценариев, по семействам, сколько с откликом; список того, что нужно
  от Андрея (H-04) с точными именами файлов.
  Готово, когда: отчёт в `out/dataset-inventory.json`, письмо для H-04 составлено.
- [ ] **U-10a · Карта синтетики в витрине** — C.
  Файлы: `frontend/src/data/validators.ts`, `frontend/src/data/validators.artifacts.test.ts`.
  Сделать: перечислить все артефакты с `synthetic: true` / `provenance: synthetic-demo` и все места
  интерфейса, которые их читают; добавить тест, который **сейчас** падает в production-сборке и
  помечен `todo` до U-10.
  Готово, когда: список в отчёте, тест написан.
- [ ] **U-13 · Читаемость графа** — C.
  Файлы: `frontend/src/views/shared/graphModel.ts`, `frontend/src/views/FieldProjection/EdgeLayer.tsx`.
  Сделать: порог рёбер по умолчанию такой, что видимых рёбер ≤ 400 при 103 узлах; толщина/прозрачность
  по весу через токены; тест на число видимых рёбер для `graph.json`.
  Готово, когда: тест зелёный, скриншот в отчёте.
- [ ] **U-11 · Единицы на карточках Обзора** — C.
  Файлы: `frontend/src/views/Overview/OverviewCard.tsx`, `frontend/src/i18n/{ru,en}/overview.json`.
  Готово, когда: у каждой величины единица из словаря; тест рендера.

**Гейт W0:** `python -m pytest -q` зелёный, включая `tests/architecture` (кроме skip без данных), фронт 1280+ тестов зелёный,
`data/base_case/response.json` существует, время прогона известно.

### W1 — коридор компенсации и кейс (04.09)

Цель: базовое расписание проходит валидатор в режиме `hard` с измеренным коридором; кейс читается из
файла.

- [ ] **P-01 · Компенсация в пластовых условиях и виды нарушений** — A.
  Файлы: новые `backend/domain/physics/__init__.py`, `backend/domain/physics/pvt.py`,
  `backend/domain/physics/voidage.py`; `backend/domain/schedule/validate.py` (`ViolationKind`),
  `backend/domain/schedule/validate_dynamic.py` (`_check_compensation`), `backend/core/contracts/constraints.py`
  (`compensation_basis`).
  Сделать: чтение `PVTO`/`PVTW` → `B_o(p)`, `B_w(p)`, `Pb`; `reservoir_compensation(step)` по §4.1;
  проверка коридора по полю и участкам; `hard`/`diagnostic`.
  Готово, когда: на базовом отклике компенсация посчитана двумя способами (PVT и `FVIR/FVPR`) и
  расхождение ≤ 1 % записано в отчёт; при `hard` и коридоре §4.1 базовое расписание без нарушений;
  при коридоре 0.95…1.15 — 224 нарушения `COMPENSATION_ABOVE_CORRIDOR`.
  Тест: `backend/domain/physics/tests/test_pvt.py`, `test_voidage.py`;
  `backend/domain/schedule/tests/test_validate_dynamic.py` — новые кейсы.
- [ ] **P-02 · Измерить коридор и записать в конфигурацию** — A.
  Файлы: `config/competition-constraints.json`, новый `config/README.md`,
  `backend/presentation/cli/physics.py` (команда `compensation-envelope`).
  Сделать: по `data/base_case/field_series.json` посчитать `min/max/median C_base(k)`, записать коридор
  `[min − 0.05, max + 0.05]`, `compensation_basis: reservoir`, `compensation_enforcement: hard`.
  Готово, когда: конфигурация обновлена, `config/README.md` объясняет происхождение каждого числа,
  `frontend/public/data/timeline.json.field_norms` пересобран (передача: C пересобирает витрину
  командой экспорта, не правит JSON руками).
- [ ] **P-03 · Убрать кап по воде из значений по умолчанию, оставить `n`** — A.
  Файлы: `config/competition-constraints.json`, `backend/core/contracts/constraints.py`,
  `backend/application/optimization/water_baseline_run.py` (удалить `WATER_SAFETY_FACTOR` и
  проекцию), `backend/application/optimization/schedule_search.py` (лимит закачки из коридора).
  Сделать: `water_reinjection_fraction` не обязателен и по умолчанию отсутствует; `external_water_m3_per_day`
  документирован как `n`; поиск использует `C_min·V_prod ≤ Σq_wi ≤ min(лимит кейса, C_max·V_prod)`.
  Готово, когда: тесты `application/optimization` зелёные, `grep WATER_SAFETY_FACTOR backend` пуст.
- [ ] **M-01 · Протокол `Agent` и реестр** — B.
  Файлы: новые `backend/domain/policy/agents/__init__.py`, `base.py`, `registry.py`, `field.py`,
  `group.py`, `well.py`; `backend/domain/policy/hierarchy.py` (делегирует реестру).
  Сделать: §5.3; существующее поведение иерархии не меняется (снимок `HierarchyTrace` до/после на
  фикстуре совпадает по хешу).
  Готово, когда: `test_hierarchy.py` зелёный без правок ожиданий; реестр из трёх агентов;
  `ARCHITECTURE_MAS.md` заведён с генерируемой таблицей (пока три строки).
  Тест: `backend/domain/policy/tests/test_agents_registry.py`.
- [ ] **M-02 · Координатор читает коридор и `n`, распорядитель — только проверенные рёбра** — B.
  Файлы: `backend/domain/policy/agents/field.py`, `group.py`, `backend/domain/policy/rules/r5.py`,
  `backend/domain/policy/state.py` (`RuleContext.compensation`, `RuleContext.edge_verified`).
  Сделать: §4.2 и §5.2; R5 читает коридор кейса; `edge_verified` пока всегда `True` (до P-12).
  Готово, когда: тест «при коридоре кейса [1.15, 1.51] R5 не трогает базу; при [0.95, 1.15] режет
  закачку до 1.15·V_prod» зелёный.
- [ ] **O-03 · Кейс из файла** — B.
  Файлы: новый `backend/application/cases.py`, `backend/presentation/cli/run.py` (`--case`),
  `config/cases/base.json`, `config/cases/README.md`.
  Сделать: §7.3; неизвестное поле — ошибка; `new_wells` — отказ с текстом.
  Готово, когда: `run search --case config/cases/base.json` проходит до выбора кандидата на
  фикстурах; тесты `tests/presentation/test_run_cli.py` расширены.
- [ ] **U-03 · Типизированные ограничения кейса** — C.
  Файлы: `frontend/src/views/Scenarios/{constraints.ts,InfrastructureTable.tsx,useEditor.ts,parseDocument.ts}`,
  новые `frontend/src/views/Scenarios/PhysicsFields.tsx`, `PhysicsFields.css`,
  `frontend/src/i18n/{ru,en}/scenarios.json`, `frontend/src/api/types.ts` (`InfrastructureDoc`).
  Сделать: §8.3; формат экспорта = `config/cases/*.json` (передача: B публикует пример файла в
  начале волны).
  Готово, когда: импорт `config/cases/base.json` в редактор и экспорт обратно дают тот же документ
  (тест round-trip); ошибки валидации через словарь.
- [ ] **U-07a · Контракт физических индикаторов Trust** — C.
  Файлы: `frontend/src/api/types.ts`, `frontend/src/data/validators.ts`,
  `frontend/src/ui/TrustBoard/indicators.ts`.
  Сделать: типы для `physics.json` (передача: A публикует схему при P-01: компенсация по шагам с
  коридором, давление, баланс), индикатор компенсации с тремя состояниями.
  Готово, когда: индикатор рендерится на фикстуре, тест на три состояния.

**Гейт W1:** базовое расписание — `sound` при `hard`; `grep -r water_reinjection_fraction config`
показывает только документацию; полный прогон тестов зелёный.

### W2 — давление, баланс, настоящий Совет (05.09)

- [ ] **P-04 · `FIPNUM` в диагностическом деке и чтение `RPR`** — A.
  Файлы: `backend/infrastructure/opm/opm_deck.py` (`emit(..., diagnostics=DeckDiagnostics)`),
  новый `backend/infrastructure/opm/regions.py`, `backend/infrastructure/opm/base_run.py`.
  Сделать: include с `FIPNUM` из `FIP_ZONE` (0 → 1), ключи `RPR`, `ROIP`, `RWIP` по регионам; чтение по
  `(KEYWORD, NUMS)`.
  Готово, когда: базовый прогон с диагностикой даёт `RPR` для 6 регионов на 371 дату, хеш
  расписания и `wells_schedule.inc` неизменны.
  Тест: `backend/infrastructure/opm/tests/test_regions.py` (skip без Docker) + разбор фикстуры.
- [ ] **P-05 · Политика давления в валидаторе** — A.
  Файлы: `backend/domain/physics/pressure.py`, `backend/core/contracts/constraints.py`
  (`pressure_floor_bar`, `pressure_ceiling_bar`, `pressure_enforcement`), `validate_dynamic.py`
  (`_check_pressure(field: FieldSeries | None)`), `validate.py` (4 вида).
  Сделать: §4.3; отсутствие серии при включённой политике — `DynamicValidationError`.
  Готово, когда: на базовом отклике нарушений нет; синтетическая серия с падением ниже пола даёт
  `FIELD_PRESSURE_BELOW_FLOOR` с шагом и значением; `_PRESSURE_BLOWUP_FACTOR` удалён.
- [ ] **P-06 · Материальный баланс как вид нарушения** — A.
  Файлы: `validate_dynamic.py`, `validate.py` (`MATERIAL_BALANCE_BROKEN`), `base_run.py` (перенос).
  Готово, когда: базовый отклик даёт относительные ошибки < 1 %, тест на сломанную серию.
- [ ] **P-07 · BHP-пределы и потолок по нефти из кейса** — A.
  Файлы: `constraints.py` (`bhp_producer_min_bar`, `bhp_injector_max_bar`, `oil_limits`),
  `validate_dynamic.py` (`check_bhp_limits` читает кейс, по умолчанию 50/300 из дека; `_check_oil_limits`
  по годам), `validate.py` (`OIL_LIMIT_EXCEEDED`).
  Готово, когда: тест с кейсом `bhp_injector_max_bar: 250` находит нарушения на базе там, где BHP > 250;
  кейс с `oil_limits` ниже базовой добычи даёт `OIL_LIMIT_EXCEEDED` с годом и значением.
- [ ] **O-11 · Оценка сдвига дат ввода скважин** — B.
  Файлы: `backend/domain/schedule/validate.py` (`FIXED_LAYER_CHANGED`), `backend/domain/schedule/emit.py`,
  `backend/domain/schedule/lossless.py`; отчёт в этом файле §7.3.
  Сделать: перечислить 22 скважины с вводом после 01.01.2007 и их блоки `COMPDAT`/`WCONPROD`/`WCONINJE`;
  оценить, что нужно, чтобы перенести ввод на другую дату без потери round-trip; решить «делаем в W5 /
  объявляем отказом» с обоснованием.
  Готово, когда: решение записано в §7.3 с оценкой в часах; если «делаем» — O-12 поставлен в W5.
- [ ] **M-03 · Экспорт настоящего `HierarchyTrace` в витрину** — B.
  Файлы: `backend/presentation/ui_export/hierarchy_view.py` (переписать без `demo_rng`),
  `backend/presentation/ui_export/demo.py`, `frontend/public/data/hierarchy.json` (пересборка).
  Сделать: уровни FIELD/GROUP/WELL из `HierarchyTrace` прогона политики на базовом отклике; поле
  `agents` — реестр (имя, уровень, ответственность, сработал ли на шаге).
  Готово, когда: `hierarchy.json.meta.provenance` — идентификатор прогона, `synthetic: false`; суммы
  участков сходятся с полем (тест); `demo_rng` не импортируется из `hierarchy_view.py`.
- [ ] **M-04 · Ни одна уставка не обходит проекцию** — B.
  Файлы: `backend/domain/policy/agents/projection.py` (`project_to_hard_constraints`),
  `backend/application/optimization/schedule_search.py`, тест `test_projection_gate.py`.
  Готово, когда: единственная точка записи `ControlEvent` в расписание — после проекции; тест
  подменяет проекцию и убеждается, что все события прошли через неё.
- [ ] **M-09 · Группы на измеренной λ и отчёт** — B.
  Файлы: `backend/domain/connectivity/groups.py`, `backend/application/connectivity_groups.py`
  (новый), тест на мини-поле из двух изолированных блоков.
  Готово, когда: `GroupingReport` печатается командой; на мини-поле два участка; на Model_Z — сколько
  получилось, с объяснением в отчёте.
- [ ] **U-04 · Совет на настоящих данных и панель «Агенты»** — C.
  Файлы: `frontend/src/views/Council/{Council.tsx,GroupLevel.tsx,WellLevel.tsx,levels.ts}`, новые
  `AgentsPanel.tsx`, `AgentsPanel.css`, `frontend/src/i18n/{ru,en}/council.json`,
  `frontend/src/data/validators.ts` (`agents`).
  Готово, когда: экран рендерит `hierarchy.json` из M-03 (передача: B публикует схему в начале волны),
  панель агентов показывает сработавших на шаге; тест.
- [ ] **U-06 · «Как посчитаны рёбра»** — C.
  Файлы: `frontend/src/views/FieldProjection/ProjectionControls.tsx`, `frontend/src/i18n/{ru,en}/projection.json`.
  Готово, когда: `InfoHint` показывает лаг, устойчивость, ранг, число обусловленности из `graph.json.meta`;
  тест.

**Гейт W2:** `hierarchy.json` без синтетики; валидатор проверяет давление и баланс на базе; полный
прогон зелёный.

### W3 — карты и карточка суррогата (06.09)

- [ ] **P-08 · `RPTRST` и читатель `UNRST`** — A.
  Файлы: `backend/infrastructure/opm/opm_deck.py` (`restart=True`), новый
  `backend/infrastructure/opm/restart_reader.py`, `base_run.py`.
  Сделать: §4.6; `PRESSURE`, `SWAT`, `SGAS` по активным ячейкам на каждую report-дату; отображение в
  (i, j, k) по `ACTNUM` из `backend/infrastructure/opm/grid_reader.py` (минимальная версия — только
  `ACTNUM`; полный читатель в P-10).
  Готово, когда: базовый прогон с диагностикой даёт 371 кадр × 240 664 ячейки; тест на фикстуре из
  двух кадров.
- [ ] **P-09 · Экспорт карт для витрины** — A.
  Файлы: новый `backend/presentation/ui_export/field_maps.py`, `frontend/public/data/base/maps/*`,
  `frontend/public/data/base/maps.json`.
  Сделать: §4.6; схема `maps.json` публикуется в начале волны (передача C).
  Готово, когда: 3 свойства × 2 пласта × 225 шагов, размер ≤ 15 МБ на сценарий, `meta.provenance` —
  прогон; тест на квантование и обратимость `min/max`.
- [ ] **S-01 · `SURROGATE.md`** — B.
  Файлы: `SURROGATE.md`, `backend/ml/surrogate/README.md` (ссылка).
  Сделать: §6.2 из чекпойнта и кода; числа — только воспроизведённые S-02 либо с пометкой даты.
  Готово, когда: документ читается за 5 минут любым участником; ссылки в бэктиках существуют.
- [ ] **S-02 · Воспроизведение метрик** — B.
  Файлы: новый `backend/ml/surrogate/report.py` (CLI), `data/surrogate/metrics.json`.
  Сделать: три таблицы §6.2 (4) по датасету из H-04; сравнение с CRM (`compare_to_baseline`).
  Готово, когда: `metrics.json` с провенансом (чекпойнт, датасет, сплит, seed); если датасета нет —
  команда завершается ошибкой с текстом, а не пустой таблицей.
- [ ] **S-03 · Физические инварианты прогноза** — B.
  Файлы: новый `backend/ml/surrogate/physics_checks.py`, тесты.
  Готово, когда: семь проверок §6.3 реализованы, на базовом отклике как «прогнозе» — ноль флагов, на
  испорченном — соответствующие флаги.
- [ ] **U-01 · Экран «Карты»** — C.
  Файлы: новые `frontend/src/views/FieldMaps/{FieldMaps.tsx,FieldMaps.css,MapCanvas.tsx,useMapFrames.ts,
  MapLegend.tsx,MapControls.tsx,decode.ts,index.ts}`, `frontend/src/i18n/{ru,en}/maps.json`,
  `frontend/src/theme/tokens.{light,dark}.css` (`--color-map-*`), `frontend/src/app/useWorkspaceRouting.ts`
  (`field/maps`), `frontend/src/ui/WorkspaceNav/*`.
  Сделать: §8.1 на фикстуре по схеме P-09.
  Готово, когда: рендер кадра, переключение свойства и пласта, синхронизация с `TimelineContext`,
  состояние «карты не рассчитаны»; тесты; ≤250 строк на файл.
- [ ] **U-02 · Легенда, подсказка, плеер, доступность** — C.
  Файлы: те же + `frontend/src/theme/tokens.test.ts` (контраст подписей шкалы).
  Готово, когда: клавиатурная навигация по ячейкам (общий `useCellKeyboard`), подсказка со значением
  и единицей, `prefers-reduced-motion`; тесты.

**Гейт W3:** карты базового сценария открываются в браузере с настоящими данными; `SURROGATE.md`
готов к отправке куратору (H-03); полный прогон зелёный.

### W4 — геология и первый пакет OPM (07.09)

- [ ] **P-10 · Читатель геологии дека** — A.
  Файлы: `backend/infrastructure/opm/grid_reader.py` (полный: `ACTNUM`, `NTG`, `PORO`, `PERMX/Y/Z`,
  `MULTX/Y/Z`, `EQLNUM`, `SATNUM`, `FIP_*`, `PINCHREG`, `MINPV`, `FAULTS`, `MULTFLT`, `MULTREGT`,
  `NNC`), новый `backend/domain/geology/__init__.py`, `model.py` (типы).
  Готово, когда: Model_Z читается за ≤ 60 с, суммы по `ACTNUM` = 240 664 / 306 974, `EQLNUM` даёт
  {1, 4, 13, 14, 15}; мини-дек с `FAULTS`/`MULTFLT` разбирается (тест).
- [ ] **P-11 · Карта барьеров** — A.
  Файлы: `backend/domain/geology/barriers.py`, тесты.
  Сделать: §4.5 (2).
  Готово, когда: на Model_Z построена карта барьеров по граням столбцов; на мини-деке с разломом
  барьер совпадает с заданной линией.
- [ ] **P-12 · Барьеры на рёбрах λ** — A.
  Файлы: `backend/domain/geology/crossings.py`, `backend/core/contracts/connectivity.py`
  (`Lambda.edge_flags` или отдельный артефакт `EdgeAudit`), `backend/presentation/ui_export/graph_view.py`
  (передача B: только поля `barrier_crossings`, `verified`).
  Готово, когда: для каждого ребра `barrier_crossings` и `verified`; отчёт «рёбра через барьер»
  в `out/geology-audit.json`; `RuleContext.edge_verified` (M-02) читает эти флаги.
- [ ] **O-01 · Incumbent из кейса** — B.
  Файлы: новый `backend/application/optimization/incumbent.py`.
  Сделать: базовое расписание → проекция на ограничения кейса → канонизация → OPM → sound.
  Готово, когда: для `config/cases/base.json` incumbent = база с тем же хешем; для кейса с простоем
  скважины — простой применён, статический валидатор чист.
- [ ] **O-02 · Генератор локальных возмущений** — B.
  Файлы: новый `backend/application/optimization/perturbations.py`.
  Сделать: §7.1 — уровни отбора/закачки, перераспределение по λ (`verified` рёбра), ветки циклики и
  переводов как отдельные семейства; каждое возмущение детерминировано по seed.
  Готово, когда: 12 кандидатов для базы строятся за секунды, все проходят статический валидатор и
  проекцию.
- [ ] **O-04 · Пакетный запуск OPM и журнал бюджета** — B.
  Файлы: новый `backend/application/optimization/opm_batch.py`, `out/opm-budget.jsonl`.
  Сделать: параллельность из конфигурации (по умолчанию 3), кеш по хешу дека, запись времени и
  статуса каждого прогона.
  Готово, когда: пакет из 12 кандидатов запущен на ночь, журнал пишется.
- [ ] **O-07 · Цикл trust-region** — B.
  Файлы: новый `backend/application/optimization/trust_region_run.py`, `backend/presentation/cli/run.py`.
  Сделать: §7.1 целиком; incumbent обновляется только при sound и `ЧДД > incumbent + ε`.
  Готово, когда: один полный раунд на фикстурах (мок OPM) проходит тестом; настоящий раунд запущен.
- [ ] **U-05 · Слой барьеров** — C.
  Файлы: новые `frontend/src/views/FieldProjection/BarrierLayer.tsx`, `BarrierLayer.css`,
  `frontend/src/data/validators.ts` (`geology.json`), `frontend/src/api/types.ts`, токен
  `--color-barrier`.
  Готово, когда: барьеры рисуются по схеме A (передача: схема `geology.json` в начале волны), рёбра
  через барьер пунктирные с подписью в инспекторе; тесты.
- [ ] **U-14 · Валидаторы новых артефактов** — C.
  Файлы: `frontend/src/data/validators.ts`, `validators.artifacts.test.ts`, `validators.hostile.test.ts`.
  Готово, когда: `maps.json`, `geology.json`, `physics.json`, `hierarchy.json.agents` проверяются;
  враждебные фикстуры отклоняются с причиной.
- [ ] **U-12 · Деньги: incumbent против базы и референса** — C.
  Файлы: `frontend/src/views/Money/MoneyComparison.tsx`, `frontend/src/i18n/{ru,en}/npv.json`.
  Готово, когда: у каждого числа подпись провенанса; тест.

**Гейт W4:** `geology.json` и `out/geology-audit.json` существуют; первый пакет OPM в журнале с
результатами; полный прогон зелёный.

### W5 — априор, второй пакет, Джарвис (08.09)

- [ ] **P-13 · Геологический априор** — A.
  Файлы: `backend/domain/geology/prior.py`, тесты.
  Готово, когда: для всех пар (добывающая, нагнетательная) — гармоническая `PERMX·NTG` вдоль пути и
  доля активных; отчёт «λ высокая, априор нулевой» в `out/geology-audit.json`.
- [ ] **P-14 · Мини-дек с разломом и экспорт `geology.json`** — A.
  Файлы: `backend/infrastructure/opm/tests/fixtures/mini_fault/*.inc`, новый
  `backend/presentation/ui_export/geology_view.py`.
  Готово, когда: сквозной тест «дек с разломом → барьер → ребро через барьер `unverified`»; экспорт для
  витрины с провенансом.
- [ ] **P-15 · Команда физического отчёта** — A.
  Файлы: `backend/presentation/cli/physics.py` (`report --run <id>`), `out/runs/<id>/validation/physics.json`.
  Готово, когда: один JSON с компенсацией, давлением, балансом, барьерами и флагами суррогата на
  прогон; его читает Trust (U-07).
- [ ] **S-04 · Инварианты в контуре отбора** — B.
  Файлы: `backend/application/optimization/schedule_search.py` (`make_evaluator`), `trust_region_run.py`.
  Готово, когда: кандидат с флагом не попадает в OPM-пакет; в журнале записана причина.
- [ ] **S-06 · OOD-порог по кандидатам оптимизатора** — B.
  Файлы: `backend/ml/surrogate/ood.py`, `data/surrogate/metrics.json`.
  Готово, когда: порог откалиброван на 40 кандидатах G10 (если данные есть) и записан с провенансом;
  иначе — ошибка с текстом.
- [ ] **M-10 · Циклика как ветка кандидатов** — B.
  Файлы: `perturbations.py`, `backend/domain/policy/rules/r7.py`, `flags.py`.
  Готово, когда: семейство `CYCLING` в пакете W5; R7 включается только для кандидата, не глобально;
  результат OPM — в журнале.
- [ ] **O-12 · Сдвиг дат ввода скважин по кейсу** — B (если O-11 решил «делаем»).
  Файлы: `backend/application/cases.py` (`commissioning_shifts`), `backend/domain/schedule/emit.py`,
  `backend/domain/schedule/validate.py`, `backend/application/optimization/incumbent.py`.
  Готово, когда: кейс с переносом ввода одной скважины на 12 месяцев раньше эмитируется, статический
  валидатор чист, OPM-прогон проходит, `wells_schedule.inc` содержит перенесённые блоки.
- [ ] **O-07b · Второй пакет trust-region** — B.
  Готово, когда: 12 прогонов, включая ветку циклики; incumbent обновлён или причина в журнале.
- [ ] **R-06 · Проверка ветки `jarvis` перед слиянием** — C.
  Сделать: прогнать тесты ветки, `tsc`, сборку; сверить границы модуля (§8 `JARVIS.md`); список
  конфликтов с `main` после W0–W4.
  Готово, когда: отчёт для человека (H-07) с командами слияния и списком конфликтов.
- [ ] **U-08 · Джарвис в `main`: знания и статус** — C (после H-07).
  Файлы: `frontend/public/jarvis/knowledge/*.md` (новые статьи §8.8), `frontend/src/jarvis/**`.
  Готово, когда: вопросы «что такое компенсация», «почему коридор 1.15–1.51», «что такое барьер»
  отвечаются из базы знаний с подсветкой нужного экрана; тесты фикстурного транспорта.
- [ ] **U-09 · «Объяснить» на карточке скважины и строке Совета** — C (бэкенд-часть M-07 — B).
  Файлы: `frontend/src/views/WellCard/TraceBlock.tsx`, `frontend/src/views/Council/WellRow.tsx`,
  `frontend/src/jarvis/actions/*`.
  Готово, когда: действие открывает сцену Джарвиса с фактами журнала; сторож провенанса пропускает
  только числа из `trace.json`; тест.
- [ ] **M-07 · Инструмент `explain_decision` для Джарвиса** — B.
  Файлы: `backend/application/jarvis/tools/decisions.py` (новый), `backend/application/jarvis/tools/registry.py`.
  Готово, когда: инструмент возвращает факты `HierarchyTrace` по (скважина, шаг) с провенансом;
  без записи — ошибка `no-trace-entry`, не выдумка.

**Гейт W5:** второй пакет OPM в журнале; Джарвис в `main` (человек слил), тесты и сборка зелёные.

### W6 — агенты давления и геологии, устойчивость, чистая витрина (09.09)

- [ ] **M-05 · `PressureAgent` через точку расширения** — B.
  Файлы: `backend/domain/policy/agents/pressure.py`, `registry.py`, тест.
  Сделать: читает `FieldSeries`/`RPR` прошлого прогона incumbent, предупреждает и снижает квоту
  участка при приближении к полу/потолку.
  Готово, когда: в отчёте волны перечислены файлы ядра, которые **не** менялись; таблица агентов в
  `ARCHITECTURE_MAS.md` перегенерирована.
- [ ] **M-06 · `GeologyAgent` и генерация таблицы агентов** — B.
  Файлы: `backend/domain/policy/agents/geology.py`, новый `backend/presentation/cli/agents_table.py`,
  `ARCHITECTURE_MAS.md`.
  Готово, когда: таблица в документе совпадает с реестром (тест сравнивает).
- [ ] **M-08 · Замер масштабирования** — B.
  Файлы: `backend/domain/policy/tests/test_scaling.py`, таблица в `ARCHITECTURE_MAS.md`.
  Готово, когда: время шага политики на 103 / 206 / 412 скважинах (дублирование фонда) измерено и
  записано; рост не хуже линейного по рёбрам.
- [ ] **O-06 · Батарея устойчивости на OPM** — B.
  Файлы: `backend/domain/robustness/**`, `backend/presentation/ui_export/scenarios.py`,
  `frontend/public/data/<сценарий>/*`.
  Сделать: 8 прогонов holdout-сценариев (простой + кап закачки, квота, обводнённость); таблица
  регрета; экспорт 1–2 сценариев в витрину как замена `whatif-injection-cut`.
  Готово, когда: `scenarios.json` содержит только сценарии с провенансом прогона; слайд «ошибка не
  растёт в 100 раз» имеет числа.
- [ ] **D-06 · `PHYSICS.md`** — A.
  Готово, когда: §4 с формулами, источниками, именами функций и видов нарушений; ссылки существуют.
- [ ] **P-16 · Сверка валидатора с типами возмущений ТЗ** — A.
  Файлы: `backend/domain/schedule/tests/test_case_types.py`.
  Готово, когда: на каждый тип возмущения ТЗ (§7.3) есть тест: кейс → нарушение находится или отказ с
  причиной.
- [ ] **U-10 · Витрина без синтетики** — C.
  Файлы: `frontend/public/data/**`, `backend/presentation/ui_export/demo.py` (передача B: удалить
  `whatif`/`demo_rng` из экспорта), `frontend/src/data/validators.ts` (тест из U-10a включён).
  Готово, когда: `grep -r synthetic-demo frontend/public/data` пуст; тест production-сборки зелёный.
- [ ] **U-07 · Trust: физические индикаторы полностью** — C.
  Файлы: `frontend/src/ui/TrustBoard/*`.
  Готово, когда: шесть индикаторов §8.5 на `physics.json` из P-15; тесты на три состояния каждого.
- [ ] **U-15 · Аудиты новых экранов** — C.
  Сделать: `impeccable` и `ui-ux-pro-max` по Картам, Совету, Ограничениям, Полю с барьерами; исправить
  найденное; полный прогон — человек.
  Готово, когда: отчёт аудита с закрытыми пунктами.

**Гейт W6:** витрина без синтетики; `ARCHITECTURE_MAS.md` с пятью агентами и замером; батарея в
журнале; полный прогон зелёный.

### W7 — сдача одной командой и документы (10.09)

- [ ] **O-05 · Пакет сдачи и самопроверка** — B.
  Файлы: новый `backend/application/submission.py`, `backend/presentation/cli/selfcheck.py`,
  `SUBMISSION.md` (черновик для D-09).
  Готово, когда: `run full --case …` завершает `ready_to_submit` с `submission/`; `selfcheck`
  перечитывает `wells_schedule.inc` и сверяет хеш; round-trip эмиссии проверен.
- [ ] **R-04 · Docker: `run full` в контейнере** — B.
  Файлы: `docker-compose.yml` (сервис `optimize`), `docker/entrypoint.sh`, `Dockerfile`.
  Готово, когда: из чистого клона `docker compose run optimize --case config/cases/base.json`
  доходит до OPM (сокет Docker проброшен) или честно сообщает причину.
- [ ] **D-01 · `ARCHITECTURE.md`** — D.
- [ ] **D-02 · `FAQ.md`** — D. Вопросы: суррогат (архитектура, метрики, OOD); агенты и роли; как
  посчитаны рёбра; разломы и геология; компенсация и коридор; давление; вода и `n`; масштабируемость
  (две оси); LLM; воспроизводимость и хеши; ЧДД против референса и базы; что не сделано. По странице на
  вопрос, числа с провенансом. Обязательные исправления того, что мы сами сказали 31.08:
  (а) суррогат **не** «принимает решения по связности» и **не** ранжирует скважины — он аппроксимирует
  отклик симулятора и ранжирует кандидатов расписания; λ измеряется планом эксперимента, а не
  суррогатом; (б) «MAE 40 миллионов рублей» и «0.52 % ЧДД» — разные метрики на разных выборках,
  в FAQ остаётся одна таблица с указанием выборки для каждого числа; (в) «разломы не учитываются» →
  «разломов в деке нет, барьеры учтены так: …» со ссылкой на `out/geology-audit.json`.
- [ ] **D-03 · `ARCHITECTURE_MAS.md`** финальная редакция — D (таблица генерируется, текст правится).
- [ ] **D-04 · Диаграмма архитектуры решения** — D. Файл: `context/assets/system-dataflow.svg`
  обновить + PNG для слайда.
- [ ] **D-05 · Доказательства масштабируемости на слайд** — D. Из M-05, M-08, P-14.
- [ ] **D-08 · `README.md` и запуск в одну команду** — D.
- [ ] **D-09 · `SUBMISSION.md`** — D.
- [ ] **U-16 · Демо-маршрут и резервное видео** — C.
  Файлы: `frontend/public/data/demo-script.json`, `frontend/src/ui/CommandPalette/commands.ts`.
  Готово, когда: маршрут §9.2 (8) проходится за 2 минуты; видео записано.
- [ ] **R-05 · Уборка репозитория** — C готовит список, H удаляет.
  Кандидаты: `ui/web` (208 МБ), `tools/g10_*`, `to-delete/`, `__pycache__`, `status.sh` (проверить).
  Готово, когда: список с обоснованием в отчёте; после удаления `tests/architecture` зелёные.

**Гейт W7:** из чистого клона `run full --case config/cases/base.json` даёт `ready_to_submit`;
документы перечитаны вторым человеком; полный прогон зелёный.

### W8 — заморозка и защита (11.09, утро)

- [ ] **O-09 · Холодный повтор ×3** — B. Чистый клон, Docker, тот же кейс → тот же хеш расписания и
  ЧДД в пределах допуска симулятора. Результат в `SUBMISSION.md`.
- [ ] **D-07 · Питч и runbook** — D. `checkpoints/2026-09-11-final-pitch.md`,
  `2026-09-11-runbook.md` по §9.2 и §9.5.
- [ ] **U-17 · Сборка и проверка витрины** — C. `npm run build`, все экраны открыты на сдаваемом
  сценарии, скриншоты в `checkpoints/`.
- [ ] **H-06 · Репетиция ×3** — команда.
- [ ] **H-01 · Зеркало в GitLab организаторов и тег куратора** — Кай (первое зеркало — конец W1,
  затем каждая волна).

### W9–W10 — резерв (если волны W0–W8 закрылись раньше)

- [ ] **O-10 · Абляция правил на OPM** — B. 2–3 правила × 1 прогон, `ablation.json` с провенансом.
- [ ] **S-07 · Переобучение с ранговым членом на восстановленном датасете** — B. Только если H-04
  закрыт к W3 и остаётся ≥ 1 день; иначе карточка с числами 31.08.
- [ ] **P-17 · Компенсация по участкам во времени на экране Обзор** — C.
- [ ] **R-08 · ESLint + Prettier** — C. Только после заморозки кода.
- [ ] **U-18 · Нефтенасыщенность и разность карт «сценарий − база»** — C.

### Задачи человека (H)

- [ ] **H-01** Заполнить GitLab организаторов, отметить куратора в беседе (после гейта W1, потом каждая волна).
- [ ] **H-02** Отправить запись чекпоинта в чат.
- [ ] **H-03** Написать куратору про суррогат (текст из `SURROGATE.md`, после W3).
- [ ] **H-04** Получить у Андрея датасет 700 прогонов (список файлов из S-05) — до W3.
- [ ] **H-05** Следить за публикацией тестового кейса; при получении — `config/cases/<кейс>.json` и запуск O-07.
- [ ] **H-06** Три репетиции по runbook.
- [ ] **H-07** Слить ветку `jarvis` в `main` после R-06 (начало W5).
- [ ] **H-08** Убрать `stash@{0}` в `aios`, закоммитить 5 staged-файлов в `docs`.

---

## 11. Риски и запасные пути

| Риск | Признак | Запасной путь |
|---|---|---|
| Прогон Model_Z дольше 30 мин | P-00 | параллелизм 4, пакеты по 8, ветку циклики отложить в W9 |
| Датасет 700 не получен | H-04 не закрыт к W3 | `SURROGATE.md` с числами 31.08 и пометкой; S-06/S-07 не делаются; на защите говорим прямо |
| λ не восстановлена | O-08 | кампания 8 прогонов в W2 вместо части пакета W4 |
| Коридор из тестового кейса отличается от измеренного | H-05 | коридор — параметр кейса; incumbent пересчитывается O-01 автоматически |
| Ни один кандидат не лучше базы под `hard` | журнал O-07 | сдаём базу, спроецированную на кейс, с OPM-подтверждением; слайд честно показывает это |
| Слияние `jarvis` конфликтует с W0–W4 | R-06 | сливать в начале W5, конфликты только в `main.tsx`, `WorkspaceNav.tsx`, `dictionaries.ts` |
| `UNRST` слишком большой | P-08 | `RPTRST BASIC=2 FREQ=…` на управляющие даты; либо только `PRESSURE`+`SWAT` |
| Региональные ключи `RPR` не читаются OPM для `FIP_ZONE` | P-04 | `FIPNUM` в диагностическом деке — уже план; при отказе OPM — регионы из `EQLNUM` |
| Группировка даёт один участок | M-09 | честно; демонстрация на мини-поле; архитектура не меняется |
| Агент отчитался «сделано» без чисел | §0.4 | задача не отмечается; перепроверка человеком до гейта |

---

## 12. Чеклист сдачи (11.09)

- [ ] `out/runs/<id>/submission/wells_schedule.inc` эмитирован из канонического расписания incumbent.
- [ ] `claimed_npv.json` = ЧДД OPM того же расписания; хеши расписания, дека, нормативов, образа OPM.
- [ ] Холодный повтор ×3 совпал.
- [ ] `validation/` без блокирующих нарушений: компенсация (`hard`), давление, баланс, BHP, лимиты кейса, простои.
- [ ] Витрина собрана из этого прогона, ни одного `synthetic`.
- [ ] Документы: `README.md`, `ARCHITECTURE.md`, `ARCHITECTURE_MAS.md`, `PHYSICS.md`, `SURROGATE.md`, `SUBMISSION.md`, `FAQ.md`, `config/README.md`.
- [ ] Docker-сборка проходит из чистого клона.
- [ ] Репозиторий организаторов = `main`, куратор отмечена.
- [ ] Слайды и runbook; резервное видео демо; владельцы вопросов знают свои страницы FAQ.
- [ ] Список «что не сделали и почему» — одна строка на пункт, без оправданий.

---

## 13. Словарь для ответов на защите

- **Компенсация (voidage replacement)** — отношение закачанного объёма к отобранному в пластовых
  условиях за шаг; у нас `reservoir_compensation()`, коридор — параметр кейса.
- **Коридор закачки** — допустимый интервал компенсации; выход выше — рост давления и риск трещины,
  ниже — падение давления и потеря добычи.
- **`n`** — дополнительный объём воды, который организаторы разрешают закачивать сверх добытой;
  параметр `external_water_m3_per_day`.
- **Барьер** — граница региона равновесия, выклинивание, полоса неактивных ячеек, множитель
  проводимости < 1, разлом; у нас карта барьеров и флаг `verified` на рёбрах λ.
- **Суррогат** — аппроксиматор отклика симулятора: ранжирует кандидатов расписания и оценивает риск;
  **не** измеряет связность и **не** выбирает скважины — на чекпоинте мы описали его неверно.
- **λ** — коэффициент влияния закачки нагнетательной на накопленную жидкость добывающей с лагом,
  измеренный планом эксперимента на возмущённых прогонах OPM, устойчивость проверена двумя партиями.
- **Incumbent** — последнее расписание, подтверждённое OPM как sound с лучшим ЧДД; только оно сдаётся.
- **Sound** — прогон завершён, тождества баланса сходятся, блокирующих нарушений нет.
- **OOD** — расстояние кандидата от области обучения суррогата; выше порога — прогноз не используется.
- **Проекция на жёсткие ограничения** — единственная точка, через которую предложения агентов
  превращаются в уставки; ничто её не обходит.
